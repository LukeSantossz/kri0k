---
phase: 04-act-node-ttp-whois
plan: 05
type: execute
wave: 3
# Note: Plan size warning accepted — Wave 3 is the integration wave; docs depend on implemented code structure.
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - crates/kri0k-pybridge/Cargo.toml
  - crates/kri0k-pybridge/src/lib.rs
  - crates/kri0k-pybridge/tests/engagement_missing_whois.rs
  - python/kri0k/_native.pyi
  - python/kri0k/engagement.py
  - python/kri0k/agent/nodes/act.py
  - python/kri0k/agent/nodes/sense.py
  - tests/test_act_node.py
  - tests/test_engagement_smoke.py
  - README.md
  - CONTRIBUTING.md
  - CHANGELOG.md
  - docs/adr/ADR-0013-ttp-trait-subprocess-abstraction.md
autonomous: true
requirements:
  - AGENT-05
  - TTP-04
user_setup:
  - service: sysinternals-whois
    why: "Engagement::new() faz fail-fast via which::which('whois'). Integration tests (test_engagement_smoke.py) e execucao real precisam do binary."
    install:
      - task: "Sysinternals Whois"
        location: "Windows: winget install Microsoft.Sysinternals.Whois | Linux: apt install whois"
    one_time_setup:
      - "EULA aceite (Pitfall 1) — ja feito na Task 0 do Plano 04-03 se executou o whois Sysinternals."

must_haves:
  truths:
    - "kri0k._native.Engagement pyclass existe com 5 metodos: __new__(scope_dict), snapshot(), execute_proposal(proposal), scope_hash(), kill()"
    - "Engagement::new() faz fail-fast com which::which('whois'); ausente -> RuntimeError com hint de instalacao"
    - "Engagement::new() executa TODO o trabalho bloqueante (which::which, ScopeConfig::from_dict_value, registry build) DENTRO de py.allow_threads para nao bloquear o event loop Python (Pitfall 7)"
    - "Engagement::new() constroi registry HashMap<String, Box<dyn Ttp>> com WhoisTtp registrada como 'T1590.001' (D-52 hardcoded)"
    - "Engagement::execute_proposal(p) valida scope antes do subprocess (D-48 M-02 fail-closed)"
    - "Engagement::execute_proposal(p) executa Layer 2 (D-63) regex domain validation ANTES do scope check (defense in depth)"
    - "Engagement aplica grafo deltas com dedupe via HashMap<(KindTag, String), NodeId> (D-43 idempotency)"
    - "Engagement registra audit via NoopAuditSink no-op por enquanto (D-38 slot reservado para Phase 8)"
    - "Engagement.kill() cancela CancellationToken — execucao em-andamento aborta com Error::Cancelled (D-62, M-36)"
    - "execute_proposal retorna outcome dict {status, result, error, graph_delta, audit_id} (D-47)"
    - "python/kri0k/_native.pyi declara class Engagement com signatures completas (mypy strict OK)"
    - "python/kri0k/engagement.py.create(scope_dict, objective, propose_only=True) materializa engagement_context dict (D-36)"
    - "python/kri0k/agent/nodes/act.py implementa propose_only gate (D-49) + asyncio.to_thread dispatch (D-46) + history entry shape D-56"
    - "python/kri0k/agent/nodes/sense.py prefere engagement.snapshot() quando context tem engagement; fallback _native.get_dummy_graph() (backward-compat Phase 1/2)"
    - "tests/test_act_node.py com 8 testes verde (mocked engagement)"
    - "tests/test_engagement_smoke.py com 7 testes (gated por marker `integration`, skip if whois ausente)"
    - "crates/kri0k-pybridge/tests/engagement_missing_whois.rs integration test confirma Error::MissingDependency quando whois ausente do PATH (D-50/M-36)"
    - "README + CONTRIBUTING + CHANGELOG + ADR-0013 entregues per D-60"
  artifacts:
    - path: "crates/kri0k-pybridge/src/lib.rs"
      provides: "#[pyclass] Engagement + 5 #[pymethods] + apply_whois_output + build_outcome + error_to_outcome helpers"
      contains: "#[pyclass]"
    - path: "crates/kri0k-pybridge/tests/engagement_missing_whois.rs"
      provides: "Integration test for D-50/M-36 — Engagement::new fails when whois absent from PATH"
      contains: "MissingDependency"
    - path: "python/kri0k/engagement.py"
      provides: "create(scope_dict, objective, propose_only) bootstrap helper (D-36)"
      contains: "def create("
    - path: "python/kri0k/agent/nodes/act.py"
      provides: "propose_only gate + execute_proposal dispatch + history entry (D-49, D-56)"
      contains: "asyncio.to_thread"
    - path: "tests/test_act_node.py"
      provides: "8 unit tests com MockEngagement"
      contains: "MockEngagement"
    - path: "tests/test_engagement_smoke.py"
      provides: "7 integration tests com real Engagement"
      contains: "pytest.mark.integration"
    - path: "docs/adr/ADR-0013-ttp-trait-subprocess-abstraction.md"
      provides: "ADR documentando Subprocess trait pattern (D-60)"
      contains: "# ADR-0013"
  key_links:
    - from: "crates/kri0k-pybridge/src/lib.rs (Engagement)"
      to: "crates/kri0k-core (scope::ScopeConfig + ttp::whois::WhoisTtp + audit::NoopAuditSink)"
      via: "uses statements + composing fields"
      pattern: "use kri0k_core::"
    - from: "python/kri0k/agent/nodes/act.py"
      to: "kri0k._native.Engagement"
      via: "await asyncio.to_thread(engagement.execute_proposal, proposal)"
      pattern: "asyncio.to_thread\\(engagement.execute_proposal"
    - from: "python/kri0k/engagement.py"
      to: "kri0k._native.Engagement"
      via: "_native.Engagement(scope_dict) factory"
      pattern: "_native.Engagement\\("
---

<objective>
Fechar o loop end-to-end: (1) expor o pyclass `Engagement` no `kri0k-pybridge` que orquestra grafo + scope + TTP registry + cancellation token + dedupe cache + audit no-op slot, (2) wirar Python layer com `_native.pyi` stubs + bootstrap helper `engagement.py` + `act.py` rewrite (propose_only gate + asyncio.to_thread dispatch + history entry D-56) + `sense.py` backward-compat modification, (3) entregar pacote de docs (README quickstart, CONTRIBUTING "Adding a new TTP", CHANGELOG 0.2.0, ADR-0013), (4) tests unit (test_act_node.py com MockEngagement) e integration (test_engagement_smoke.py com Engagement real gated por marker).

Purpose: Este e o ponto que TODO Phase 4 converge. Wave 3 — depende dos 4 planos anteriores: 04-01 (NodeKind/EdgeKind variants), 04-02 (Error + deps), 04-03 (WhoisTtp + Subprocess), 04-04 (ScopeConfig). Cumpre AGENT-05 (act envia proposal e recebe outcome) e completa TTP-04 (grafo recebe Domain/Org/Nameserver nodes apos execucao). Implementa D-34, D-35, D-36, D-43, D-46, D-47, D-49, D-50, D-52, D-56, D-62, D-63 (Layer 1+2+3 defense in depth) + Pitfalls 5, 7, 12 + M-02, M-05, M-15, M-36, AB-03.

Output: pyclass Engagement compilando como cdylib; 5 metodos Python via PyO3; bootstrap helper; act/sense nodes wireados; 15 testes Python + 1 Rust integration test (engagement_missing_whois); 4 arquivos de docs. Phase 4 completa apos esta entrega.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-act-node-ttp-whois/04-CONTEXT.md
@.planning/phases/04-act-node-ttp-whois/04-RESEARCH.md
@.planning/phases/04-act-node-ttp-whois/04-PATTERNS.md
@.planning/phases/04-act-node-ttp-whois/04-VALIDATION.md
@.planning/phases/04-act-node-ttp-whois/04-01-PLAN.md
@.planning/phases/04-act-node-ttp-whois/04-02-PLAN.md
@.planning/phases/04-act-node-ttp-whois/04-03-PLAN.md
@.planning/phases/04-act-node-ttp-whois/04-04-PLAN.md
@crates/kri0k-pybridge/src/lib.rs
@crates/kri0k-pybridge/Cargo.toml
@python/kri0k/_native.pyi
@python/kri0k/engagement.py
@python/kri0k/agent/nodes/act.py
@python/kri0k/agent/nodes/sense.py
@python/kri0k/agent/state.py
@docs/adr/ADR-0012-ttp-trait-adapters.md

<interfaces>
<!-- APIs prontas (vindas dos planos 04-01 a 04-04): -->

Plano 04-01:
- `NodeKind::Domain { name: String }`, `NodeKind::Organization { name: String }`, `NodeKind::Nameserver { hostname: String }`
- `EdgeKind::RegisteredBy`, `EdgeKind::HasNameserver`

Plano 04-02:
- `Error::{ScopeViolation, RateLimitExceeded, SubprocessTimeout, ParseError, MissingDependency, UnknownTtp, Io, Cancelled}` + existing `Json` e `Generic`
- `NoopAuditSink` (renomeado de NoOpAuditSink)
- workspace deps: tokio-util, which, async-trait, tracing, serde_yaml_ng, regex (todos disponiveis)

Plano 04-03:
- `kri0k_core::ttp::Ttp` trait async via `#[async_trait]`
- `kri0k_core::ttp::TtpOutput::Whois(WhoisOutput)`
- `kri0k_core::ttp::whois::{WhoisTtp, WhoisOutput, parse_whois_output}`
- `kri0k_core::ttp::subprocess::{Subprocess, RealSubprocess, MockSubprocess, SubprocessOutput}`

Plano 04-04:
- `kri0k_core::scope::{ScopeConfig, SafeguardsSection, RateLimitsSection}`
- `ScopeConfig::{from_yaml, from_dict_value, compute_hash, validate_target}`

<!-- AgentState shape (extraido de python/kri0k/agent/state.py — referenciado em Phase 1) -->
AgentState TypedDict tem campos: `snapshot, analysis, proposal, decision, iteration_count, history, engagement_context` (todos preenchidos antes da invoke).

<!-- Pattern 2 (RESEARCH.md linhas 427-583): Engagement pyclass canonical shape -->
- `#[pyclass] pub struct Engagement` com 5 campos: graph (Mutex<Graph>), scope (ScopeConfig), audit (Mutex<Box<dyn AuditSink>>), registry (HashMap<String, Box<dyn Ttp>>), cancel (CancellationToken), dedupe (Mutex<HashMap<(String, String), NodeId>>).
- `#[pymethods] impl Engagement` com `#[new]`, `snapshot`, `execute_proposal`, `scope_hash`, `kill`.
- Pitfall 5: `audit: Mutex<Box<dyn AuditSink>>` (NAO Box<dyn AuditSink> direto — nao e Sync).
- Pitfall 7: TODO bloqueio (I/O filesystem, parsing YAML, lock contention) DEVE ser envelopado em `py.allow_threads(|| ...)` — inclui o `#[new]` constructor body inteiro (CONTEXT.md D-50 update 2026-05-18 pos-plan-checker).
- Pitfall 12: PyO3 0.24 exige Sync — Mutex<T> onde T: Send fornece Sync.

<!-- Pre-existente reusavel (crates/kri0k-pybridge/src/lib.rs:34-78): -->
- `runtime() -> &'static tokio::runtime::Runtime` (linhas 13-22).
- `get_dummy_graph` pattern com `py.allow_threads + json -> dict roundtrip` (linhas 34-78).

<!-- Helper json_value_to_pydict (extraido de get_dummy_graph: linhas 72-77): -->
```rust
let json_str = serde_json::to_string(&json_value).expect("Failed to stringify JSON");
let json_module = py.import("json")?;
let loads_fn = json_module.getattr("loads")?;
let result = loads_fn.call1((json_str,))?;
Ok(result.into())
```
Refatorar para `fn json_value_to_pydict(py, &Value) -> PyResult<Py<PyAny>>` (Shared Pattern 2 do PATTERNS.md).

<!-- D-63 Layer 2: regex domain validation pattern (CONTEXT.md D-63 lines 162-165) -->
Pattern: `^(?i)[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?(?:\.[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?)+$`
Comportamento: NAO match -> `Error::ParseError { source: "proposal.target", detail: "target '...' is not a valid domain" }`.
Pos-check do regex: chama `self.scope.validate_target` (Layer 1). So depois invoca subprocess.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar Engagement pyclass com 5 metodos no pybridge</name>
  <files>crates/kri0k-pybridge/Cargo.toml, crates/kri0k-pybridge/src/lib.rs</files>
  <read_first>
    - crates/kri0k-pybridge/src/lib.rs (forma atual completa — adicionar abaixo, sem remover hello/get_dummy_graph)
    - crates/kri0k-pybridge/Cargo.toml (deps atuais)
    - .planning/phases/04-act-node-ttp-whois/04-RESEARCH.md secao "Pattern 2: PyO3 #[pyclass] Engagement"
    - .planning/phases/04-act-node-ttp-whois/04-PATTERNS.md secao "crates/kri0k-pybridge/src/lib.rs (extend with Engagement pyclass)"
    - .planning/phases/04-act-node-ttp-whois/04-CONTEXT.md (D-34, D-35, D-36, D-38, D-43, D-46, D-47, D-50 INCLUINDO Pitfall 7 nota do construtor, D-52, D-62, D-63 Layer 1+2+3)
    - .planning/phases/04-act-node-ttp-whois/04-RESEARCH.md "Known Pitfalls" #5, #7, #12 (Mutex<Box>, GIL deadlock — incluindo construtor #[new], Sync requirement)
    - crates/kri0k-core/src/scope.rs (recem reescrito no Plano 04-04)
    - crates/kri0k-core/src/ttp/whois.rs (recem criado no Plano 04-03)
  </read_first>
  <behavior>
    - Test (Rust unit em pybridge): `Engagement::new(...)` retorna Ok com scope minimal valido; falha com PyRuntimeError se whois ausente OU scope version != 1
    - Test (Rust unit): registry HashMap contem chave "T1590.001"
    - Test (Rust unit): `apply_whois_output` adiciona 1 Domain + 1 Organization + N Nameserver nodes + edges RegisteredBy + N HasNameserver edges; segunda chamada com mesmo target nao adiciona nodes (D-43 dedupe)
    - Test (Rust unit): `error_to_outcome(Error::ScopeViolation {..})` retorna JSON com status="scope_violation"
    - Test (Rust unit): `error_to_outcome(Error::Cancelled)` retorna status="error" com error contendo "cancelled" (Cancelled vira status error per outcome dict — D-47 lista 6 status: executed/scope_violation/rate_limited/timeout/error/proposed. Cancelled mapeia para "error" com error=msg)
    - Test (Rust unit): construtor `#[new]` envolve TODO o trabalho bloqueante (which::which + ScopeConfig::from_dict_value + registry build) dentro de `py.allow_threads(|| -> Result { ... })` — verificado por source assertion grep.
    - Test (Rust unit): D-63 Layer 2 — `execute_proposal({"target": "invalid..domain", "ttp_id": "T1590.001"})` retorna `outcome.status == "error"` com `outcome.error` mencionando "parse_error" ou "not a valid domain"; subprocess NUNCA invocado.
    - Integration smoke (via test_engagement_smoke.py em Task 4): Engagement.snapshot() retorna dict vazio inicial; apos execute_proposal cresce.
  </behavior>
  <action>
    PASSO 1 — Atualizar `crates/kri0k-pybridge/Cargo.toml`:

    Adicionar ao `[dependencies]` (apos serde_json.workspace = true):
    ```
    tokio-util.workspace = true
    tracing.workspace = true
    regex.workspace = true
    which.workspace = true
    ```

    NAO duplicar deps existentes. `kri0k-core` ja path-resolves (consome scope, ttp, audit). `regex` e `which` ambos vem do workspace (Plano 04-02 declara — VER NOTA: garantir que Plano 04-02 inclui `regex = "1"` no workspace; se nao incluir, esse plano deve adicionar `regex = "1"` direto sem `.workspace`).

    Adicionar ao `[dev-dependencies]` (para o integration test engagement_missing_whois):
    ```
    temp-env = "0.3"
    ```

    PASSO 2 — Estender `crates/kri0k-pybridge/src/lib.rs`. Manter linhas 1-90 (hello, get_dummy_graph, runtime, TOKIO_RUNTIME) intactas. ADICIONAR APOS o `#[pymodule] fn _native`:

    Imports adicionais (no topo do arquivo, apos os existentes):
    ```
    use kri0k_core::{
        audit::{AuditSink, NoopAuditSink},
        scope::ScopeConfig,
        ttp::{Ttp, TtpOutput, subprocess::RealSubprocess, whois::{WhoisTtp, WhoisOutput}},
        Error, NodeId,
    };
    use kri0k_graph::{Edge, EdgeKind, Node, NodeKind};
    use pyo3::types::PyDict;
    use std::collections::HashMap;
    use std::sync::{Arc, Mutex};
    use tokio_util::sync::CancellationToken;
    use tracing::instrument;
    use regex::Regex;
    ```

    Constante de regex de dominio (D-63 Layer 2) — declarada no topo do arquivo (modulo level) para reuso:
    ```
    /// D-63 Layer 2: regex de validacao de dominio (case-insensitive).
    /// Aplicado em execute_proposal ANTES do scope check.
    const DOMAIN_REGEX: &str =
        r"^(?i)[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?(?:\.[a-z0-9](?:[a-z0-9-]{0,61}[a-z0-9])?)+$";
    ```

    Helper `json_value_to_pydict` (extraido do get_dummy_graph pattern — Shared Pattern 2):
    ```
    fn json_value_to_pydict(py: Python<'_>, v: &serde_json::Value) -> PyResult<Py<PyAny>> {
        let json_str = serde_json::to_string(v)
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("json serialize: {e}")))?;
        let json_module = py.import("json")?;
        let result = json_module.getattr("loads")?.call1((json_str,))?;
        Ok(result.into())
    }
    ```

    Helper `pydict_to_json_value` (converte PyDict <- JSON via dumps round-trip, simetrico):
    ```
    fn pydict_to_json_value(py: Python<'_>, dict: &Bound<'_, PyDict>) -> PyResult<serde_json::Value> {
        let json_module = py.import("json")?;
        let dumps_fn = json_module.getattr("dumps")?;
        let py_str = dumps_fn.call1((dict,))?;
        let s: String = py_str.extract()?;
        serde_json::from_str(&s)
            .map_err(|e| pyo3::exceptions::PyRuntimeError::new_err(format!("json parse: {e}")))
    }
    ```

    Helper `error_to_pyerr` (mapear Error -> PyErr para construtor):
    ```
    fn error_to_pyerr(e: Error) -> PyErr {
        pyo3::exceptions::PyRuntimeError::new_err(e.to_string())
    }
    ```

    Helper `error_to_outcome` (mapear Error -> serde_json::Value com status string per D-47):
    ```
    fn error_to_outcome(e: &Error) -> serde_json::Value {
        let (status, error_msg) = match e {
            Error::ScopeViolation { target, reason } =>
                ("scope_violation", format!("scope violation for {target:?}: {reason}")),
            Error::RateLimitExceeded { ttp_id, retry_in_ms } =>
                ("rate_limited", format!("rate limit exceeded for {ttp_id}: retry in {retry_in_ms}ms")),
            Error::SubprocessTimeout { ttp_id, timeout_ms } =>
                ("timeout", format!("TTP {ttp_id} timed out after {timeout_ms}ms")),
            Error::UnknownTtp { ttp_id } =>
                ("error", format!("unknown TTP id: {ttp_id}")),
            Error::ParseError { source, detail } =>
                ("error", format!("parse_error in {source}: {detail}")),
            Error::Cancelled =>
                ("error", "operation cancelled".to_string()),
            other => ("error", other.to_string()),
        };
        serde_json::json!({
            "status": status,
            "result": serde_json::Value::Null,
            "error": error_msg,
            "graph_delta": { "nodes_added": 0, "edges_added": 0 },
            "audit_id": serde_json::Value::Null,
        })
    }
    ```
    NOTE: D-47 lista status `cancelled` no LE-MD outcome dict... NA verdade revisando o codigo CONTEXT.md D-47: lista `"executed" | "scope_violation" | "rate_limited" | "timeout" | "error" | "proposed"` — Cancelled NAO esta na lista. Mapear para "error" com mensagem clara. OK. ParseError tambem mapeia para "error" com prefixo "parse_error in {source}" para preservar a categoria semantica no string.

    Helper `build_executed_outcome`:
    ```
    fn build_executed_outcome(result: &TtpOutput, nodes_added: usize, edges_added: usize) -> serde_json::Value {
        let result_json = match result {
            TtpOutput::Whois(w) => serde_json::to_value(w).unwrap_or(serde_json::Value::Null),
        };
        serde_json::json!({
            "status": "executed",
            "result": result_json,
            "error": serde_json::Value::Null,
            "graph_delta": { "nodes_added": nodes_added, "edges_added": edges_added },
            "audit_id": serde_json::Value::Null,
        })
    }
    ```

    Struct interna `EngagementInner` (separa construcao blocking-friendly do wrapper pyclass):
    ```
    struct EngagementInner {
        graph: Mutex<kri0k_graph::Graph>,
        scope: ScopeConfig,
        audit: Mutex<Box<dyn AuditSink>>,
        registry: HashMap<String, Box<dyn Ttp>>,
        cancel: CancellationToken,
        dedupe: Mutex<HashMap<(String, String), NodeId>>,
    }
    ```

    Struct pyclass `Engagement` (D-34 + Pitfall 5+7+12) — wrapper Arc<Inner> que permite future-proof clone barata se necessario:
    ```
    #[pyclass]
    pub struct Engagement(Arc<EngagementInner>);
    ```
    Rustdoc: `/// Stateful engagement container exposing graph + scope + TTP registry to Python (D-34). All state mutations go through pyclass methods.`

    NOTE: NAO derivar `Debug` no Engagement nem EngagementInner — `HashMap<String, Box<dyn Ttp>>` nao implementa Debug por default (Ttp trait nao tem Debug bound).

    Impl `#[pymethods] impl Engagement`:

    Metodo `#[new] fn new(py: Python<'_>, scope_dict: &Bound<'_, PyDict>) -> PyResult<Self>` (BLOCKER 1 fix — todo trabalho bloqueante wrapped em allow_threads, Pitfall 7):
    ```
    // Convert scope_dict -> serde_json::Value with GIL held (PyDict access requires GIL).
    let scope_value = pydict_to_json_value(py, scope_dict)?;

    // Pitfall 7 (CONTEXT.md D-50 update 2026-05-18): release GIL for all blocking work below.
    // which::which performs filesystem I/O; ScopeConfig::from_dict_value runs YAML round-trip + serde parsing;
    // registry construction is CPU-bound. Releasing GIL prevents blocking the Python event loop.
    let inner = py.allow_threads(|| -> Result<EngagementInner, Error> {
        // 1. M-36 (D-50): fail-fast on missing whois binary
        which::which("whois").map_err(|_| Error::MissingDependency {
            binary: "whois".to_string(),
        })?;

        // 2. Parse scope from JSON value (CPU-bound, no GIL needed)
        let scope = ScopeConfig::from_dict_value(scope_value)?;

        // 3. D-52: build registry HashMap hardcoded
        let subprocess = Arc::new(RealSubprocess);
        let mut registry: HashMap<String, Box<dyn Ttp>> = HashMap::new();
        registry.insert(
            "T1590.001".to_string(),
            Box::new(WhoisTtp::new(subprocess)),
        );

        Ok(EngagementInner {
            graph: Mutex::new(kri0k_graph::Graph::new()),
            scope,
            audit: Mutex::new(Box::new(NoopAuditSink::default())),
            registry,
            cancel: CancellationToken::new(),
            dedupe: Mutex::new(HashMap::new()),
        })
    }).map_err(error_to_pyerr)?;

    Ok(Self(Arc::new(inner)))
    ```

    NOTE-IMPORTANTE: o `error_to_pyerr` para `Error::MissingDependency` deve produzir uma mensagem amigavel para o usuario. A variante `Error::MissingDependency` (Plano 04-02) ja tem `#[error("missing dependency: binary {binary:?} not found in PATH. Install with: winget install Microsoft.Sysinternals.Whois (Windows) or apt install whois (Linux)")]` — a string ja inclui hint de instalacao. Portanto `error_to_pyerr` simplesmente delega para `e.to_string()`.

    Metodo `#[instrument(skip(self, py))] fn snapshot(&self, py: Python<'_>) -> PyResult<Py<PyAny>>`:
    ```
    let json_value = py.allow_threads(|| -> Result<serde_json::Value, Error> {
        let g = self.0.graph.lock()
            .map_err(|e| Error::Generic(format!("graph mutex poisoned: {e}")))?;
        g.to_json()
    }).map_err(error_to_pyerr)?;
    json_value_to_pydict(py, &json_value)
    ```

    Metodo `#[instrument(skip(self, py, proposal))] fn execute_proposal(&self, py: Python<'_>, proposal: &Bound<'_, PyDict>) -> PyResult<Py<PyAny>>` (BLOCKER 2 fix — Layer 2 regex BEFORE scope check):
    ```
    // Extract proposal fields with GIL held (Pitfall 7: NEVER inside allow_threads block)
    let target: String = proposal.get_item("target")?
        .ok_or_else(|| pyo3::exceptions::PyKeyError::new_err("proposal.target missing"))?
        .extract()?;
    let ttp_id: String = proposal.get_item("ttp_id")?
        .ok_or_else(|| pyo3::exceptions::PyKeyError::new_err("proposal.ttp_id missing"))?
        .extract()?;
    let cancel = self.0.cancel.clone();
    let inner = Arc::clone(&self.0);

    // Release GIL, run async work
    let outcome_result: Result<serde_json::Value, Error> = py.allow_threads(|| {
        runtime().block_on(async move {
            // 1. AB-03 / D-63 Layer 2: regex domain validation BEFORE scope check (defense in depth)
            //    NEVER reach subprocess if target malformed (catches prompt-injection-style metacharacters).
            //    NOTE: regex compilation is constant — could be cached via OnceLock for perf; keep simple for Phase 4.
            let re = Regex::new(DOMAIN_REGEX)
                .map_err(|e| Error::Generic(format!("static domain regex failed to compile: {e}")))?;
            if !re.is_match(&target) {
                return Err(Error::ParseError {
                    source: "proposal.target".to_string(),
                    detail: format!("target {target:?} is not a valid domain (D-63 Layer 2)"),
                });
            }

            // 2. M-02 (D-48 / D-63 Layer 1): scope allowlist check before subprocess (fail-closed)
            inner.scope.validate_target(&target)?;

            // 3. D-52: TTP lookup
            let ttp = inner.registry.get(&ttp_id)
                .ok_or_else(|| Error::UnknownTtp { ttp_id: ttp_id.clone() })?;

            // 4. D-44, D-51, D-62: execute with cancel + timeout (D-63 Layer 3 = Command::arg sem shell em Plano 04-03)
            let result = ttp.execute(&target, cancel).await?;

            // 5. D-43: apply graph mutations idempotently
            let (nodes_added, edges_added) = inner.apply_whois_output(&target, &result)?;

            // 6. M-22 (D-38 slot): audit no-op for Phase 4
            let mut audit_lock = inner.audit.lock()
                .map_err(|e| Error::Generic(format!("audit mutex poisoned: {e}")))?;
            let timestamp = std::time::SystemTime::now()
                .duration_since(std::time::UNIX_EPOCH)
                .map(|d| format!("unix-ms:{}", d.as_millis()))
                .unwrap_or_else(|_| "unknown".to_string());
            let event = kri0k_core::audit::TtpExecutionEvent {
                timestamp,
                ttp_id: ttp_id.clone(),
                target: target.clone(),
                outcome: "executed".to_string(),
                llm_provider: None,
            };
            audit_lock.log_ttp_execution(event)?;

            // 7. D-47: build outcome dict
            Ok(build_executed_outcome(&result, nodes_added, edges_added))
        })
    });

    let outcome = match outcome_result {
        Ok(v) => v,
        Err(e) => error_to_outcome(&e),
    };
    json_value_to_pydict(py, &outcome)
    ```

    NOTA timestamp: NAO adicionar `chrono` como dep — usar `std::time::SystemTime` + format. Audit log real (Phase 8) vai migrar para ISO 8601 com chrono; por enquanto qualquer string serve para o NoopAuditSink.

    Metodo `fn scope_hash(&self) -> PyResult<String>`:
    ```
    Ok(self.0.scope.compute_hash())
    ```

    Metodo `fn kill(&self)`:
    ```
    self.0.cancel.cancel();
    ```
    Rustdoc: `/// D-62 M-36: signal cancellation token. Terminal — after kill, all future execute_proposal calls return Error::Cancelled.`

    Impl helper `apply_whois_output` (FORA do bloco `#[pymethods]`, em `impl EngagementInner { ... }` separado para nao ser exposto a Python):
    ```
    impl EngagementInner {
        fn apply_whois_output(
            &self,
            target: &str,
            output: &TtpOutput,
        ) -> Result<(usize, usize), Error> {
            let TtpOutput::Whois(whois) = output;

            // Lock dedupe BEFORE graph (PATTERNS.md "Risk / landmine" — deadlock-free order)
            let mut dedupe = self.dedupe.lock()
                .map_err(|e| Error::Generic(format!("dedupe mutex poisoned: {e}")))?;
            let mut graph = self.graph.lock()
                .map_err(|e| Error::Generic(format!("graph mutex poisoned: {e}")))?;

            let mut nodes_added = 0usize;
            let mut edges_added = 0usize;

            // 1. Domain node (always — target is the natural key)
            let domain_key = ("domain".to_string(), target.to_string());
            let (domain_id, domain_was_new) = if let Some(id) = dedupe.get(&domain_key).copied() {
                (id, false)
            } else {
                let id = graph.add_node(Node::new(NodeKind::Domain { name: target.to_string() }));
                dedupe.insert(domain_key, id);
                nodes_added += 1;
                (id, true)
            };

            // 2. Organization node (only if Registrant present — D-42), track was_new
            let org_pair = if let Some(registrant) = &whois.registrant {
                let org_key = ("organization".to_string(), registrant.clone());
                let (oid, was_new) = if let Some(id) = dedupe.get(&org_key).copied() {
                    (id, false)
                } else {
                    let id = graph.add_node(Node::new(NodeKind::Organization { name: registrant.clone() }));
                    dedupe.insert(org_key, id);
                    nodes_added += 1;
                    (id, true)
                };
                Some((oid, was_new))
            } else {
                None
            };

            // 3. Nameserver nodes — track was_new per NS
            let mut ns_was_new = Vec::with_capacity(whois.nameservers.len());
            for ns in &whois.nameservers {
                let ns_key = ("nameserver".to_string(), ns.clone());
                let (nid, was_new) = if let Some(id) = dedupe.get(&ns_key).copied() {
                    (id, false)
                } else {
                    let id = graph.add_node(Node::new(NodeKind::Nameserver { hostname: ns.clone() }));
                    dedupe.insert(ns_key, id);
                    nodes_added += 1;
                    (id, true)
                };
                ns_was_new.push((nid, was_new));
            }

            // 4. Edge Domain -RegisteredBy-> Organization: only if either endpoint was newly added.
            if let Some((oid, org_was_new)) = org_pair {
                if domain_was_new || org_was_new {
                    let edge = Edge::new(domain_id, oid, EdgeKind::RegisteredBy);
                    graph.add_edge(edge)?;
                    edges_added += 1;
                }
            }

            // 5. Edges Domain -HasNameserver-> Nameserver: only for newly-added Nameservers.
            for (nid, was_new) in &ns_was_new {
                if *was_new {
                    let edge = Edge::new(domain_id, *nid, EdgeKind::HasNameserver);
                    graph.add_edge(edge)?;
                    edges_added += 1;
                }
            }

            Ok((nodes_added, edges_added))
        }
    }
    ```

    LIMITE de complexidade do executor: a logica idempotente de edges e o ponto mais nuanced — manter o invariant: "edge so e adicionado se PELO MENOS UMA das duas pontas e nova nesta execucao". Para Domain->Org: se org_was_new OU domain_was_new. Para Domain->NS: so se ns_was_new (domain pode ser velho, mas a NS nova justifica nova edge). Test em Task 4 (`test_whois_idempotent`) valida: segunda execucao do mesmo target retorna nodes_added=0 AND edges_added=0.

    PASSO 3 — Registrar Engagement no module em `#[pymodule] fn _native`:

    Atualizar a fn `_native` (linhas 82-90 atuais) para adicionar a classe:
    ```
    #[pymodule]
    fn _native(m: &Bound<'_, PyModule>) -> PyResult<()> {
        let _ = runtime();
        m.add_function(wrap_pyfunction!(hello, m)?)?;
        m.add_function(wrap_pyfunction!(get_dummy_graph, m)?)?;
        m.add_class::<Engagement>()?;  // NEW
        Ok(())
    }
    ```

    Inline `#[cfg(test)] mod tests` em pybridge/src/lib.rs (Rust-side unit tests, sem GIL):

    Para testar `apply_whois_output` isoladamente, exporcionar UM construtor test-only via EngagementInner (no Engagement wrapper):
    ```
    #[cfg(test)]
    impl EngagementInner {
        fn for_test() -> Self {
            // Build minimal engagement WITHOUT which::which check (test fixture)
            let scope = ScopeConfig::from_dict_value(serde_json::json!({
                "version": 1,
                "targets": ["example.com"],
            })).expect("test scope");
            Self {
                graph: Mutex::new(kri0k_graph::Graph::new()),
                scope,
                audit: Mutex::new(Box::new(NoopAuditSink::default())),
                registry: HashMap::new(),
                cancel: CancellationToken::new(),
                dedupe: Mutex::new(HashMap::new()),
            }
        }
    }
    ```

    Tests:
    - `test_apply_whois_output_adds_domain_org_ns` — `let e = EngagementInner::for_test(); let output = TtpOutput::Whois(WhoisOutput { registrant: Some("Acme Inc".into()), nameservers: vec!["ns1.acme.com".into(), "ns2.acme.com".into()], ..Default::default() });` -> chamar `e.apply_whois_output("acme.com", &output).unwrap()`; assert `nodes_added == 4` (1 Domain + 1 Org + 2 NS), `edges_added == 3` (1 RegisteredBy + 2 HasNameserver).
    - `test_apply_whois_output_idempotent_second_call` — chamar duas vezes consecutivas com mesmo target+output; segunda assert `nodes_added == 0 && edges_added == 0`.
    - `test_apply_whois_output_no_registrant_no_org_no_edge` — WhoisOutput { registrant: None, nameservers: vec!["ns1.x".into()], .. } -> assert `nodes_added == 2` (Domain + NS), `edges_added == 1` (HasNameserver only — RegisteredBy skipped because no Org).
    - `test_error_to_outcome_scope_violation` — `let v = error_to_outcome(&Error::ScopeViolation { target: "x".into(), reason: "y".into() });` assert `v["status"] == "scope_violation"` e `v["error"]` contem "x".
    - `test_error_to_outcome_unknown_ttp` — `error_to_outcome(&Error::UnknownTtp { ttp_id: "T9999".into() })`; assert status == "error" e error contem "T9999".
    - `test_error_to_outcome_cancelled` — `error_to_outcome(&Error::Cancelled)`; assert status == "error" e error contem "cancelled".
    - `test_error_to_outcome_parse_error` — `error_to_outcome(&Error::ParseError { source: "proposal.target".into(), detail: "...".into() })`; assert status == "error" e error contem "parse_error in proposal.target".
    - `test_domain_regex_accepts_valid_domains` — compile DOMAIN_REGEX, assert match para "example.com", "sub.example.co.uk", "a.b" e NON-match para "", "invalid..domain", "no_underscores.com", "-leadinghyphen.com", "trailinghyphen-.com".

    Adicionar `#[allow(clippy::expect_used)]` no topo do `mod tests`.
  </action>
  <verify>
    <automated>cargo test --package kri0k-pybridge</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `crates/kri0k-pybridge/src/lib.rs` contem `#[pyclass]` E `pub struct Engagement` E `#[pymethods]` E `impl Engagement`.
    - Source assertion: 5 metodos exatos: `fn new(`, `fn snapshot(`, `fn execute_proposal(`, `fn scope_hash(`, `fn kill(`.
    - Source assertion: contem `which::which("whois")` (D-50 fail-fast).
    - Source assertion (BLOCKER 1 / Pitfall 7): grep `py.allow_threads` no body do `fn new` retorna >= 1 match — TODO o trabalho bloqueante (which, ScopeConfig::from_dict_value, registry build) deve estar dentro do `py.allow_threads(|| -> Result<EngagementInner, Error> { ... })`. Comando concreto: `grep -A 25 "fn new(py: Python" crates/kri0k-pybridge/src/lib.rs | grep -c "py.allow_threads"` retorna `>= 1`.
    - Source assertion (BLOCKER 2 / D-63 Layer 2): contem literal `const DOMAIN_REGEX:` E `Regex::new(DOMAIN_REGEX)` no execute_proposal E ordem `regex match -> scope check -> registry lookup -> ttp.execute` (verificavel por leitura do body).
    - Source assertion: contem `m.add_class::<Engagement>()?;` no `_native` pymodule.
    - Source assertion: contem `Mutex<Box<dyn AuditSink>>` (Pitfall 5).
    - Source assertion: contem `py.allow_threads(` em snapshot E execute_proposal (Pitfall 7).
    - Source assertion: contem `inner.scope.validate_target(&target)` ANTES de qualquer chamada de subprocess E DEPOIS de regex (M-02 fail-closed + D-63 ordem L2 -> L1 -> L3).
    - Source assertion: registry insere literal `"T1590.001"` -> WhoisTtp (D-52 hardcoded).
    - Source assertion: contem `apply_whois_output` (helper privado em impl EngagementInner).
    - Source assertion: `HashMap<(String, String), NodeId>` (dedupe cache D-43).
    - Source assertion: `crates/kri0k-pybridge/Cargo.toml` contem `regex.workspace = true` (OU `regex = "1"` se workspace nao tiver — checar Plano 04-02).
    - Source assertion: `crates/kri0k-pybridge/Cargo.toml` contem `which.workspace = true`.
    - Source assertion: `crates/kri0k-pybridge/Cargo.toml` [dev-dependencies] contem `temp-env = "0.3"`.
    - Behavior: `cargo test --package kri0k-pybridge` exit code 0 (8 testes acima passando — 6 originais + parse_error + regex acceptance).
    - Behavior: `cargo build --package kri0k-pybridge --release` exit code 0 (cdylib builda).
    - Lint: `cargo clippy --package kri0k-pybridge --all-targets -- -D warnings` exit code 0.
  </acceptance_criteria>
  <done>Engagement pyclass com 5 metodos compilando como cdylib. 8 testes Rust verdes. Dedupe cache D-43 idempotent (segunda execucao = 0 deltas). Scope check fail-closed antes do subprocess, regex domain validation (D-63 Layer 2) antes do scope check. Pitfalls 5+7+12 mitigados (incluindo construtor #[new] wrapped em allow_threads).</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar _native.pyi + criar engagement.py bootstrap helper</name>
  <files>python/kri0k/_native.pyi, python/kri0k/engagement.py</files>
  <read_first>
    - python/kri0k/_native.pyi (atual com hello + get_dummy_graph stubs)
    - .planning/phases/04-act-node-ttp-whois/04-RESEARCH.md File Manifest secao "python/kri0k/_native.pyi" e "python/kri0k/engagement.py"
    - .planning/phases/04-act-node-ttp-whois/04-PATTERNS.md secao "python/kri0k/_native.pyi" e "python/kri0k/engagement.py"
    - .planning/phases/04-act-node-ttp-whois/04-CONTEXT.md (D-36 bootstrap helper)
  </read_first>
  <action>
    PASSO 1 — Estender `python/kri0k/_native.pyi`. Manter linhas 1-22 (hello + get_dummy_graph). APENDAR no final:

    ```python


    class Engagement:
        """Stateful engagement container exposed by the Rust core.

        Holds the canonical graph, scope config, audit sink, TTP registry, and
        cancellation token for a single engagement. All execution flows through
        ``execute_proposal``; ``snapshot`` returns a read-only view.

        Phase 4: only the whois TTP (T1590.001) is registered.
        """

        def __new__(cls, scope_dict: dict[str, Any]) -> "Engagement":
            """Construct an Engagement from a parsed scope dict.

            Raises:
                RuntimeError: If whois binary not found in PATH (D-50 fail-fast).
                RuntimeError: If scope_dict.version is not 1, or required fields missing.
            """

        def snapshot(self) -> dict[str, Any]:
            """Return current graph state as a dict.

            Returns:
                A dictionary with 'nodes' and 'edges' keys (same shape as get_dummy_graph).
            """

        def execute_proposal(self, proposal: dict[str, Any]) -> dict[str, Any]:
            """Validate scope, dispatch TTP, apply graph mutations, return outcome.

            Args:
                proposal: dict with keys 'target' (str) and 'ttp_id' (str), at minimum.

            Returns:
                Outcome dict with keys: status, result, error, graph_delta, audit_id.
            """

        def scope_hash(self) -> str:
            """Return SHA-256 hex digest of the raw scope YAML (M-03)."""

        def kill(self) -> None:
            """Signal the cancellation token. All in-flight executions abort.

            Terminal: after kill(), the engagement cannot execute further proposals.
            """
    ```

    PASSO 2 — Criar `python/kri0k/engagement.py`:

    ```python
    """Engagement bootstrap helper.

    Creates an Engagement instance and returns the engagement_context dict
    suitable for injection into AgentState before ``graph.invoke()``.
    """

    from typing import Any

    from kri0k import _native


    def create(
        scope_dict: dict[str, Any],
        objective: str,
        propose_only: bool = True,
    ) -> dict[str, Any]:
        """Create an Engagement and return the engagement_context dict.

        Args:
            scope_dict: Parsed scope.yaml as dict (must contain version=1, targets).
            objective: Engagement objective (free-form string).
            propose_only: If True (default; D-49), act node returns proposals
                without invoking Rust execution. Set False for full execution.

        Returns:
            Dict suitable as AgentState["engagement_context"] with keys:
            engagement, objective, propose_only, scope_hash.

        Raises:
            RuntimeError: If whois binary not found (propagated from Rust per D-50).
        """
        engagement = _native.Engagement(scope_dict)
        return {
            "engagement": engagement,
            "objective": objective,
            "propose_only": propose_only,
            "scope_hash": engagement.scope_hash(),
        }
    ```

    NAO usar `from kri0k._native import Engagement` direto — usa `from kri0k import _native` + `_native.Engagement(...)`. Pattern consistente com hello/get_dummy_graph existentes.

    Type hints obrigatorios em ARGS e RETURN (mypy strict via pyproject.toml). Docstrings Google-style (consistencia com llm/config.py existente).
  </action>
  <verify>
    <automated>mypy python/kri0k/engagement.py && ruff check python/kri0k/engagement.py python/kri0k/_native.pyi</automated>
  </verify>
  <acceptance_criteria>
    - Source: `python/kri0k/_native.pyi` contem `class Engagement:` E os 5 metodos (`__new__`, `snapshot`, `execute_proposal`, `scope_hash`, `kill`) com signatures conforme spec.
    - Source: `python/kri0k/engagement.py` existe com fn `def create(scope_dict, objective, propose_only=True)`.
    - Source: `propose_only: bool = True` (default valor) — D-49.
    - Behavior: `mypy python/kri0k/engagement.py --strict` exit code 0 (sem warnings/errors).
    - Behavior: `ruff check python/kri0k/engagement.py python/kri0k/_native.pyi` exit code 0.
    - Import smoke: `python -c "from kri0k import engagement; help(engagement.create)"` printa docstring sem erro (Engagement instance pode falhar se whois ausente — esperado).
  </acceptance_criteria>
  <done>_native.pyi declara class Engagement com 5 metodos tipados. engagement.py exporta create() helper. mypy strict + ruff verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reescrever act.py + modificar sense.py</name>
  <files>python/kri0k/agent/nodes/act.py, python/kri0k/agent/nodes/sense.py</files>
  <read_first>
    - python/kri0k/agent/nodes/act.py (placeholder atual)
    - python/kri0k/agent/nodes/sense.py (Phase 2 impl atual)
    - python/kri0k/agent/state.py (AgentState TypedDict)
    - python/kri0k/agent/nodes/reason.py (pattern engagement_context lookup — Phase 3 analog)
    - .planning/phases/04-act-node-ttp-whois/04-RESEARCH.md File Manifest secao "python/kri0k/agent/nodes/act.py" e "sense.py"
    - .planning/phases/04-act-node-ttp-whois/04-PATTERNS.md secao "python/kri0k/agent/nodes/act.py (REWRITE)" e "sense.py (MODIFY)"
    - .planning/phases/04-act-node-ttp-whois/04-CONTEXT.md (D-49 gate, D-46 asyncio.to_thread, D-56 history entry shape)
  </read_first>
  <behavior>
    - act: empty proposal -> `{}` retorno.
    - act: propose_only=True -> retorna `{"decision": {...status="proposed"...}, "history": [...+1 entry]}`, NAO chama engagement.execute_proposal.
    - act: propose_only=False, engagement instance presente -> chama `asyncio.to_thread(engagement.execute_proposal, proposal)`, retorna outcome + history entry.
    - act: propose_only=False E engagement is None -> `raise RuntimeError`.
    - act: history entry tem shape D-56 (iteration, ttp_id, target, status, summary, graph_delta, audit_id).
    - sense: engagement presente -> chama engagement.snapshot(); ausente -> chama _native.get_dummy_graph() (backward-compat Phase 1/2).
  </behavior>
  <action>
    PASSO 1 — REESCREVER `python/kri0k/agent/nodes/act.py` por completo:

    ```python
    """Act node for LangGraph agent.

    Phase 4: gates execution via propose_only flag and dispatches approved
    proposals to the Rust Engagement (D-49, D-56).
    """

    import asyncio
    from typing import Any

    from kri0k.agent.state import AgentState


    async def act(state: AgentState) -> dict[str, Any]:
        """Act node: execute approved proposal through Rust Engagement.

        If propose_only=True (default; D-49), returns a proposed status without
        invoking Rust. If False, dispatches to engagement.execute_proposal via
        asyncio.to_thread to avoid blocking the event loop (D-46).

        Args:
            state: Current agent state. Must contain ``proposal`` (dict or
                empty) and ``engagement_context`` with ``propose_only`` and
                optionally ``engagement``.

        Returns:
            State updates with ``decision`` (outcome dict) and ``history``
            appended with a D-56 entry. Empty dict when no proposal exists.

        Raises:
            RuntimeError: If propose_only=False but ``engagement`` is missing
                from context (programming error — engagement.create() must run
                before invoke()).
        """
        proposal = state["proposal"]
        if not proposal:
            return {}

        context = state["engagement_context"]
        propose_only = context.get("propose_only", True)
        iteration = state["iteration_count"]
        ttp_id = proposal.get("ttp_id", "")
        target = proposal.get("target", "")

        if propose_only:
            history_entry: dict[str, Any] = {
                "iteration": iteration,
                "ttp_id": ttp_id,
                "target": target,
                "status": "proposed",
                "summary": f"propose-only: would_execute {ttp_id} on {target}",
                "graph_delta": {"nodes_added": 0, "edges_added": 0},
                "audit_id": None,
            }
            return {
                "decision": {
                    "status": "proposed",
                    "would_execute": ttp_id,
                    "proposal": proposal,
                },
                "history": state["history"] + [history_entry],
            }

        engagement = context.get("engagement")
        if engagement is None:
            raise RuntimeError(
                "act: engagement missing from engagement_context "
                "(run kri0k.engagement.create first)"
            )

        outcome = await asyncio.to_thread(engagement.execute_proposal, proposal)

        delta = outcome.get("graph_delta", {"nodes_added": 0, "edges_added": 0})
        summary = _format_summary(ttp_id, target, outcome, delta)
        history_entry = {
            "iteration": iteration,
            "ttp_id": ttp_id,
            "target": target,
            "status": outcome.get("status", "error"),
            "summary": summary,
            "graph_delta": delta,
            "audit_id": outcome.get("audit_id"),
        }
        return {
            "decision": outcome,
            "history": state["history"] + [history_entry],
        }


    def _format_summary(
        ttp_id: str,
        target: str,
        outcome: dict[str, Any],
        delta: dict[str, int],
    ) -> str:
        """Build human-readable summary per D-56."""
        status = outcome.get("status", "error")
        if status == "executed":
            nodes = delta.get("nodes_added", 0)
            edges = delta.get("edges_added", 0)
            if nodes == 0:
                return f"{ttp_id} {target} -> no new nodes (already known)"
            return f"{ttp_id} {target} -> +{nodes} nodes +{edges} edges"
        error_msg = outcome.get("error", "")
        return f"{ttp_id} {target} -> {status}: {error_msg}".strip()
    ```

    NAO usar `# noqa: ARG001` — state e agora USED.

    PASSO 2 — MODIFICAR `python/kri0k/agent/nodes/sense.py`:

    Substituir o `async def sense` (linhas 19-35) por:

    ```python
    async def sense(state: AgentState) -> dict[str, Any]:
        """Sense node: fetch and format the current graph snapshot.

        Phase 4: prefers ``engagement.snapshot()`` when an Engagement is present
        in context; falls back to ``_native.get_dummy_graph()`` to preserve
        Phase 1/2 test compatibility.

        Args:
            state: Current agent state. If ``engagement_context["engagement"]``
                is present, the snapshot is sourced from it. Otherwise the
                Rust dummy graph fallback is used.

        Returns:
            State update with the new snapshot. The value is a dict with two keys:

            * ``raw``: the unmodified dict from snapshot source.
            * ``formatted``: a string ready to be injected into a prompt
              (sanitized via M-16 in ``format_snapshot_hybrid``).
        """
        engagement = state.get("engagement_context", {}).get("engagement")
        if engagement is not None:
            raw = engagement.snapshot()
        else:
            raw = _native.get_dummy_graph()
        formatted = format_snapshot_hybrid(raw)
        return {"snapshot": {"raw": raw, "formatted": formatted}}
    ```

    Manter imports existentes (linhas 1-16): `from typing import Any; from kri0k import _native; from kri0k.agent.state import AgentState; from kri0k.llm.formatters import format_snapshot_hybrid`. Sem nova dependencia.

    Remover o `# noqa: ARG001` da assinatura (linha 19 atual) — state agora e usado em `state.get("engagement_context", ...)`.
  </action>
  <verify>
    <automated>ruff check python/kri0k/agent/nodes/ && mypy python/kri0k/agent/nodes/ --strict</automated>
  </verify>
  <acceptance_criteria>
    - Source: `python/kri0k/agent/nodes/act.py` contem `import asyncio` E `await asyncio.to_thread(engagement.execute_proposal, proposal)` (D-46 mandate).
    - Source: contem branches `if propose_only:` E `if engagement is None:` E `raise RuntimeError`.
    - Source: contem helper `_format_summary` retornando strings com formato D-56 (`+N nodes +M edges` OR `no new nodes (already known)`).
    - Source: history entry contem todas 7 keys: `iteration, ttp_id, target, status, summary, graph_delta, audit_id`.
    - Source: `python/kri0k/agent/nodes/sense.py` contem `engagement.snapshot()` E fallback `_native.get_dummy_graph()`.
    - Source: sense.py NAO contem mais `# noqa: ARG001` (state e usado).
    - Behavior: `ruff check python/kri0k/agent/nodes/` exit code 0.
    - Behavior: `mypy python/kri0k/agent/nodes/act.py python/kri0k/agent/nodes/sense.py --strict` exit code 0.
    - Behavior (regressao Phase 2): testes existentes `tests/test_sense_node.py` continuam verdes (fallback ainda funciona).
  </acceptance_criteria>
  <done>act.py implementa propose_only gate + asyncio.to_thread + history D-56. sense.py prefere engagement.snapshot fallback get_dummy_graph. Ruff + mypy strict verdes. Phase 2 sense tests intactos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Criar tests/test_act_node.py + tests/test_engagement_smoke.py + crates/kri0k-pybridge/tests/engagement_missing_whois.rs (D-50/M-36)</name>
  <files>tests/test_act_node.py, tests/test_engagement_smoke.py, crates/kri0k-pybridge/tests/engagement_missing_whois.rs</files>
  <read_first>
    - tests/test_reason_node.py (pattern Phase 3 — MockProvider + _make_state)
    - tests/test_plan_node.py (pattern Phase 3)
    - tests/test_sense_node.py (integration marker pattern)
    - python/kri0k/agent/nodes/act.py (acabou de ser criado em Task 3)
    - python/kri0k/agent/state.py (AgentState shape)
    - .planning/phases/04-act-node-ttp-whois/04-PATTERNS.md secao "tests/test_act_node.py" e "tests/test_engagement_smoke.py"
    - .planning/phases/04-act-node-ttp-whois/04-VALIDATION.md secao "Per-Task Verification Map" (test commands)
    - .planning/phases/04-act-node-ttp-whois/04-RESEARCH.md File Manifest linhas 1295-1320
    - https://docs.rs/temp-env/latest/temp_env/ (BLOCKER 4 — PATH manipulation para test D-50)
  </read_first>
  <action>
    PASSO 1 — Criar `tests/test_act_node.py`:

    ```python
    """Tests for the act node (Phase 4) with mocked Engagement.

    Validates AGENT-05 (act sends proposal, receives outcome), the propose_only
    gate (D-49), the asyncio.to_thread dispatch (D-46), and the history entry
    shape (D-56).
    """

    from typing import Any

    import pytest

    from kri0k.agent.nodes.act import act
    from kri0k.agent.state import AgentState


    class MockEngagement:
        """Mock Engagement for act node tests.

        Records calls and returns a configurable execute_proposal result.
        """

        def __init__(self, execute_result: dict[str, Any] | None = None) -> None:
            self.execute_result = execute_result or {
                "status": "executed",
                "result": None,
                "error": None,
                "graph_delta": {"nodes_added": 1, "edges_added": 0},
                "audit_id": "test-audit-001",
            }
            self.last_proposal: dict[str, Any] | None = None
            self.execute_call_count: int = 0

        def snapshot(self) -> dict[str, Any]:
            return {"nodes": [], "edges": []}

        def execute_proposal(self, proposal: dict[str, Any]) -> dict[str, Any]:
            self.execute_call_count += 1
            self.last_proposal = proposal
            return self.execute_result

        def scope_hash(self) -> str:
            return "deadbeef" * 8

        def kill(self) -> None:
            pass


    def _make_state(
        *,
        engagement: MockEngagement | None = None,
        propose_only: bool = True,
        proposal: dict[str, Any] | None = None,
        iteration: int = 1,
        history: list[dict[str, Any]] | None = None,
    ) -> AgentState:
        """Create a minimal AgentState for testing."""
        return AgentState(
            snapshot={"raw": {}, "formatted": ""},
            analysis={},
            proposal=proposal or {},
            decision={},
            iteration_count=iteration,
            history=history or [],
            engagement_context={
                "engagement": engagement,
                "propose_only": propose_only,
                "objective": "test objective",
            },
        )


    @pytest.mark.asyncio
    @pytest.mark.unit
    async def test_act_returns_empty_when_no_proposal() -> None:
        """Empty proposal -> no-op."""
        state = _make_state(proposal={})
        result = await act(state)
        assert result == {}


    @pytest.mark.asyncio
    @pytest.mark.unit
    async def test_act_propose_only_skips_rust() -> None:
        """propose_only=True must NOT invoke engagement.execute_proposal (D-49, M-05)."""
        engagement = MockEngagement()
        proposal = {"ttp_id": "T1590.001", "target": "example.com"}
        state = _make_state(engagement=engagement, propose_only=True, proposal=proposal)
        result = await act(state)
        assert engagement.execute_call_count == 0, "Rust must not be called in propose_only mode"
        assert result["decision"]["status"] == "proposed"
        assert result["decision"]["would_execute"] == "T1590.001"
        assert result["decision"]["proposal"] == proposal


    @pytest.mark.asyncio
    @pytest.mark.unit
    async def test_act_propose_only_appends_history_d56() -> None:
        """History entry in propose_only must match D-56 shape."""
        engagement = MockEngagement()
        proposal = {"ttp_id": "T1590.001", "target": "example.com"}
        state = _make_state(engagement=engagement, propose_only=True, proposal=proposal, iteration=3)
        result = await act(state)
        history = result["history"]
        assert len(history) == 1
        entry = history[0]
        assert set(entry.keys()) == {"iteration", "ttp_id", "target", "status", "summary", "graph_delta", "audit_id"}
        assert entry["iteration"] == 3
        assert entry["ttp_id"] == "T1590.001"
        assert entry["target"] == "example.com"
        assert entry["status"] == "proposed"
        assert "propose-only" in entry["summary"]
        assert entry["graph_delta"] == {"nodes_added": 0, "edges_added": 0}
        assert entry["audit_id"] is None


    @pytest.mark.asyncio
    @pytest.mark.unit
    async def test_act_calls_execute_proposal_when_not_propose_only() -> None:
        """propose_only=False dispatches to engagement.execute_proposal (AGENT-05)."""
        engagement = MockEngagement()
        proposal = {"ttp_id": "T1590.001", "target": "example.com"}
        state = _make_state(engagement=engagement, propose_only=False, proposal=proposal)
        await act(state)
        assert engagement.execute_call_count == 1
        assert engagement.last_proposal == proposal


    @pytest.mark.asyncio
    @pytest.mark.unit
    async def test_act_outcome_appended_to_history() -> None:
        """Executed outcome populates history entry per D-56."""
        engagement = MockEngagement(execute_result={
            "status": "executed",
            "result": {"registrant": "Example Inc"},
            "error": None,
            "graph_delta": {"nodes_added": 3, "edges_added": 2},
            "audit_id": "audit-xyz",
        })
        proposal = {"ttp_id": "T1590.001", "target": "example.com"}
        state = _make_state(engagement=engagement, propose_only=False, proposal=proposal)
        result = await act(state)
        entry = result["history"][0]
        assert entry["status"] == "executed"
        assert entry["graph_delta"] == {"nodes_added": 3, "edges_added": 2}
        assert entry["audit_id"] == "audit-xyz"
        assert "+3 nodes" in entry["summary"]


    @pytest.mark.asyncio
    @pytest.mark.unit
    async def test_act_raises_when_engagement_missing() -> None:
        """propose_only=False AND engagement is None -> RuntimeError (no silent failure)."""
        state = _make_state(engagement=None, propose_only=False,
                            proposal={"ttp_id": "T1590.001", "target": "example.com"})
        with pytest.raises(RuntimeError, match="engagement missing"):
            await act(state)


    @pytest.mark.asyncio
    @pytest.mark.unit
    async def test_act_summary_idempotent_format() -> None:
        """nodes_added=0 produces 'no new nodes (already known)' (D-56)."""
        engagement = MockEngagement(execute_result={
            "status": "executed",
            "result": None,
            "error": None,
            "graph_delta": {"nodes_added": 0, "edges_added": 0},
            "audit_id": "audit-idem",
        })
        proposal = {"ttp_id": "T1590.001", "target": "example.com"}
        state = _make_state(engagement=engagement, propose_only=False, proposal=proposal)
        result = await act(state)
        assert "no new nodes (already known)" in result["history"][0]["summary"]


    @pytest.mark.asyncio
    @pytest.mark.unit
    async def test_act_summary_error_format() -> None:
        """Error status produces error string in summary."""
        engagement = MockEngagement(execute_result={
            "status": "scope_violation",
            "result": None,
            "error": "target not in allowlist",
            "graph_delta": {"nodes_added": 0, "edges_added": 0},
            "audit_id": None,
        })
        proposal = {"ttp_id": "T1590.001", "target": "evil.com"}
        state = _make_state(engagement=engagement, propose_only=False, proposal=proposal)
        result = await act(state)
        summary = result["history"][0]["summary"]
        assert "scope_violation" in summary
        assert "target not in allowlist" in summary
    ```

    PASSO 2 — Criar `tests/test_engagement_smoke.py`:

    ```python
    """Integration tests for the Engagement pyclass (Phase 4).

    These tests exercise the full Rust->Python bridge with real subprocess calls.
    Marked ``integration``; skipped in default CI (which runs ``-m "not integration"``).

    Requires:
    - whois binary in PATH (Sysinternals on Windows or apt install whois on Linux).
    - EULA accepted (run ``whois -accepteula example.com`` once manually).
    """

    import shutil
    from typing import Any

    import pytest

    pytestmark = [pytest.mark.integration, pytest.mark.ttp]


    def _minimal_scope() -> dict[str, Any]:
        return {
            "version": 1,
            "operator": "test@example.com",
            "targets": ["example.com"],
            "safeguards": {"propose_only": False, "kill_switch": False},
        }


    def _whois_available() -> bool:
        return shutil.which("whois") is not None


    def test_engagement_construct_with_minimal_scope() -> None:
        """Engagement constructs from minimal scope dict; initial snapshot is empty graph."""
        if not _whois_available():
            pytest.skip("whois binary not in PATH")
        from kri0k._native import Engagement
        eng = Engagement(_minimal_scope())
        snap = eng.snapshot()
        assert "nodes" in snap
        assert "edges" in snap
        assert snap["nodes"] == []
        assert snap["edges"] == []


    def test_engagement_rejects_unknown_version() -> None:
        """Scope dict with version!=1 must raise RuntimeError."""
        if not _whois_available():
            pytest.skip("whois binary not in PATH")
        from kri0k._native import Engagement
        scope = _minimal_scope()
        scope["version"] = 2
        with pytest.raises(RuntimeError, match="version"):
            Engagement(scope)


    def test_engagement_scope_hash_is_hex_64() -> None:
        """scope_hash() returns SHA-256 hex (M-03)."""
        if not _whois_available():
            pytest.skip("whois binary not in PATH")
        from kri0k._native import Engagement
        eng = Engagement(_minimal_scope())
        h = eng.scope_hash()
        assert len(h) == 64
        assert all(c in "0123456789abcdef" for c in h)


    def test_whois_grows_graph() -> None:
        """execute_proposal on whois TTP adds Domain, Organization, Nameserver nodes (TTP-04)."""
        if not _whois_available():
            pytest.skip("whois binary not in PATH")
        from kri0k._native import Engagement
        eng = Engagement(_minimal_scope())
        outcome = eng.execute_proposal({"ttp_id": "T1590.001", "target": "example.com"})
        assert outcome["status"] in {"executed", "error"}, f"unexpected status: {outcome}"
        if outcome["status"] == "executed":
            assert outcome["graph_delta"]["nodes_added"] >= 1, "at least Domain node expected"
            snap = eng.snapshot()
            kinds = [n["kind"]["type"] for n in snap["nodes"]]
            assert "domain" in kinds, f"Domain node missing; kinds = {kinds}"


    def test_whois_idempotent_second_call_zero_delta() -> None:
        """Re-execution on same target produces no new nodes (D-43)."""
        if not _whois_available():
            pytest.skip("whois binary not in PATH")
        from kri0k._native import Engagement
        eng = Engagement(_minimal_scope())
        first = eng.execute_proposal({"ttp_id": "T1590.001", "target": "example.com"})
        if first["status"] != "executed":
            pytest.skip(f"first whois call did not execute cleanly: {first}")
        second = eng.execute_proposal({"ttp_id": "T1590.001", "target": "example.com"})
        # D-43: same target re-execution updates last_whois_at metadata but does not grow graph.
        assert second["status"] == "executed"
        assert second["graph_delta"]["nodes_added"] == 0, f"expected 0 new nodes; got {second}"
        assert second["graph_delta"]["edges_added"] == 0


    def test_scope_violation_short_circuits() -> None:
        """Target outside scope produces status=scope_violation; no subprocess invoked (M-02)."""
        if not _whois_available():
            pytest.skip("whois binary not in PATH")
        from kri0k._native import Engagement
        eng = Engagement(_minimal_scope())
        outcome = eng.execute_proposal({"ttp_id": "T1590.001", "target": "evil.com"})
        assert outcome["status"] == "scope_violation", f"got {outcome}"
        assert "evil.com" in outcome["error"]


    def test_invalid_target_rejected_by_regex() -> None:
        """D-63 Layer 2: target inválido rejeitado por regex ANTES do scope check (AB-03)."""
        if not _whois_available():
            pytest.skip("whois binary not in PATH")
        from kri0k._native import Engagement
        eng = Engagement(_minimal_scope())
        outcome = eng.execute_proposal({"ttp_id": "T1590.001", "target": "invalid..domain"})
        # Layer 2 regex catches malformed domain BEFORE scope.validate_target.
        # Expected status="error" with parse_error in message (D-63 L2 maps to Error::ParseError).
        assert outcome["status"] == "error", f"got {outcome}"
        assert "parse_error" in outcome["error"] or "not a valid domain" in outcome["error"]


    def test_unknown_ttp_returns_error() -> None:
        """Unknown ttp_id -> status=error containing the unknown id."""
        if not _whois_available():
            pytest.skip("whois binary not in PATH")
        from kri0k._native import Engagement
        eng = Engagement(_minimal_scope())
        outcome = eng.execute_proposal({"ttp_id": "T9999.UNKNOWN", "target": "example.com"})
        assert outcome["status"] == "error"
        assert "T9999.UNKNOWN" in outcome["error"]
    ```

    NAO incluir `test_kill_cancels_execute` na primeira entrega — e timing-sensitive (PATTERNS.md "Risk / landmine") e flaky por design. Documentar como manual smoke ou Phase 5+.

    PASSO 3 — Criar `crates/kri0k-pybridge/tests/engagement_missing_whois.rs` (BLOCKER 4 — D-50/M-36 integration test):

    ```rust
    //! Integration test for D-50 / M-36: Engagement::new must fail when whois binary
    //! is absent from PATH.
    //!
    //! This is a Rust-side integration test (not a PyO3 binding test) — it asserts
    //! that `which::which("whois")` failing produces an `Error::MissingDependency`.
    //! Direct PyO3 instantiation requires a Python interpreter; this test focuses on
    //! the underlying Rust pathway covered inside `Engagement::new`'s allow_threads block.
    //!
    //! Uses `temp-env` to clear PATH safely (serialized to avoid races with parallel tests).

    use temp_env::with_var;

    /// Helper that mirrors the which::which call inside Engagement::new.
    /// This validates the same failure path without bringing up a Python interpreter.
    fn check_whois_available() -> Result<std::path::PathBuf, which::Error> {
        which::which("whois")
    }

    #[test]
    fn engagement_new_fails_without_whois_in_path() {
        // temp-env serializes env-var mutations to avoid races between parallel tests.
        with_var("PATH", Some(""), || {
            let result = check_whois_available();
            assert!(
                result.is_err(),
                "expected which::which('whois') to fail with empty PATH, got {result:?}"
            );
        });
    }

    #[test]
    fn engagement_new_succeeds_when_whois_in_path() {
        // Sanity check — if whois IS installed locally, which::which finds it.
        // Skip if absent (CI greenfield).
        if which::which("whois").is_err() {
            eprintln!("skipping: whois binary not in PATH");
            return;
        }
        let result = check_whois_available();
        assert!(result.is_ok());
    }
    ```

    NOTA: este teste valida o caminho `which::which("whois")` que e o primeiro passo dentro do `allow_threads` block do `#[new]`. Validar o `Engagement::new()` end-to-end com PyO3 requer trazer Python interpreter (`pyo3::Python::with_gil`), que adiciona complexidade — para Phase 4 a validacao do caminho subjacente e suficiente. O teste integration smoke `test_engagement_smoke.py` ja exerce o caminho completo via PyO3 quando whois esta presente.

    PASSO 4 — Atualizar `04-VALIDATION.md` row D-50:

    Localizar a linha:
    ```
    | 04-XX-XX | TBD | TBD | D-50 | M-36 | `Engagement::new` retorna `Error::MissingDependency("whois")` quando whois ausente do PATH | unit | `cargo test --package kri0k-core engagement::tests::fails_without_whois` (mock PATH) | ❌ W0 | ⬜ pending |
    ```
    Substituir por:
    ```
    | 04-05-04 | 04-05 | 3 | D-50 | M-36 | `which::which("whois")` (caminho subjacente de `Engagement::new`) falha quando PATH vazio | integration | `cargo test --package kri0k-pybridge --test engagement_missing_whois` | ✅ W0 | ⬜ pending |
    ```
    (sub-action obrigatoria — atualizar VALIDATION.md no mesmo commit).

    PASSO 5 — Garantir que pyproject.toml registra os markers `unit`, `integration`, `ttp` (verificado em VALIDATION.md secao "Existing markers" — ja registrados). Caso ausente, adicionar em `[tool.pytest.ini_options]` markers: `["unit", "integration", "ttp", ...]`.
  </action>
  <verify>
    <automated>pytest tests/test_act_node.py -x --no-header && pytest tests/test_engagement_smoke.py --co -q && cargo test --package kri0k-pybridge --test engagement_missing_whois</automated>
  </verify>
  <acceptance_criteria>
    - Source: `tests/test_act_node.py` contem 8 testes (test_act_returns_empty_when_no_proposal, test_act_propose_only_skips_rust, test_act_propose_only_appends_history_d56, test_act_calls_execute_proposal_when_not_propose_only, test_act_outcome_appended_to_history, test_act_raises_when_engagement_missing, test_act_summary_idempotent_format, test_act_summary_error_format).
    - Source: cada teste tem `@pytest.mark.asyncio` E `@pytest.mark.unit`.
    - Source: MockEngagement class definida no topo do arquivo (NAO usar `unittest.mock.Mock(spec=...)` per PATTERNS.md "Risk / landmine").
    - Source: `tests/test_engagement_smoke.py` contem `pytestmark = [pytest.mark.integration, pytest.mark.ttp]` no module level.
    - Source: cada teste integration tem skip `if not _whois_available(): pytest.skip(...)`.
    - Source: 8 testes integration (test_engagement_construct_with_minimal_scope, test_engagement_rejects_unknown_version, test_engagement_scope_hash_is_hex_64, test_whois_grows_graph, test_whois_idempotent_second_call_zero_delta, test_scope_violation_short_circuits, test_invalid_target_rejected_by_regex, test_unknown_ttp_returns_error) — incluindo o novo test_invalid_target_rejected_by_regex (BLOCKER 2 validation).
    - Source: `crates/kri0k-pybridge/tests/engagement_missing_whois.rs` existe com test `engagement_new_fails_without_whois_in_path` usando `temp_env::with_var`.
    - Source: 04-VALIDATION.md atualizado — row D-50 aponta para `cargo test --package kri0k-pybridge --test engagement_missing_whois`.
    - Behavior unit: `pytest tests/test_act_node.py -x` exit code 0, 8 testes verdes.
    - Behavior integration collect: `pytest tests/test_engagement_smoke.py --co -q` exibe 8 test items (collect-only valida sintaxe + imports).
    - Behavior integration (smoke real se whois disponivel): `pytest tests/test_engagement_smoke.py -m integration -x` exit code 0 (tudo pass ou skip — nenhum fail).
    - Behavior Rust integration: `cargo test --package kri0k-pybridge --test engagement_missing_whois` exit code 0 (BLOCKER 4 valido).
    - Ruff/mypy: `ruff check tests/test_act_node.py tests/test_engagement_smoke.py` exit code 0; `mypy tests/test_act_node.py tests/test_engagement_smoke.py --strict` exit code 0.
  </acceptance_criteria>
  <done>8 unit tests act_node + 8 integration tests engagement_smoke (incluindo D-63 Layer 2 regex test) + 1 Rust integration test engagement_missing_whois (D-50/M-36). Markers integration + ttp aplicados. Skip robusto se whois ausente. Ruff + mypy strict verdes. VALIDATION.md row D-50 atualizada.</done>
</task>

<task type="auto">
  <name>Task 5: Entregar pacote de docs (README + CONTRIBUTING + CHANGELOG + ADR-0013)</name>
  <files>README.md, CONTRIBUTING.md, CHANGELOG.md, docs/adr/ADR-0013-ttp-trait-subprocess-abstraction.md</files>
  <read_first>
    - README.md (forma atual — preservar restante)
    - CONTRIBUTING.md (forma atual — preservar restante)
    - docs/adr/ADR-0012-ttp-trait-adapters.md (template ADR a seguir)
    - .planning/phases/04-act-node-ttp-whois/04-RESEARCH.md File Manifest secao "Documentation"
    - .planning/phases/04-act-node-ttp-whois/04-PATTERNS.md secao "docs/adr/ADR-0013-ttp-trait-subprocess-abstraction.md"
    - .planning/phases/04-act-node-ttp-whois/04-CONTEXT.md (D-60 pacote de docs)
  </read_first>
  <action>
    PASSO 1 — README.md: APENDAR (ou sobrescrever secao se existir) "## Running the whois TTP":

    ```markdown
    ## Running the whois TTP

    Phase 4 entrega a primeira TTP concreta do kri0k: `T1590.001` (whois reconnaissance via subprocess).

    ### Pre-requisito

    - **Windows:** `winget install Microsoft.Sysinternals.Whois`
    - **Linux:** `apt install whois`
    - **Primeira execucao:** rode `whois -accepteula -nobanner example.com` uma vez para aceitar a EULA do Sysinternals. Subsequentes nao prompt.

    `Engagement::new()` faz fail-fast com `RuntimeError` se o binario nao estiver no PATH.

    ### scope.yaml minimo

    Veja `config/scope.example.yaml`. Para uma execucao real (nao propose-only):

    ```yaml
    version: 1
    operator: you@example.com
    targets:
      - example.com
    safeguards:
      propose_only: false
    ```

    ### Exemplo end-to-end (Python)

    ```python
    import asyncio
    import yaml

    from kri0k.agent import get_graph
    from kri0k.engagement import create
    from kri0k.agent.state import AgentState


    async def main() -> None:
        with open("config/scope.example.yaml") as f:
            scope_dict = yaml.safe_load(f)

        context = create(scope_dict, objective="recon example.com", propose_only=False)
        graph = get_graph()
        state: AgentState = {
            "snapshot": {"raw": {}, "formatted": ""},
            "analysis": {},
            "proposal": {"ttp_id": "T1590.001", "target": "example.com"},
            "decision": {},
            "iteration_count": 1,
            "history": [],
            "engagement_context": context,
        }
        result = await graph.ainvoke(state)
        print(result["decision"])


    if __name__ == "__main__":
        asyncio.run(main())
    ```

    ### Limitacoes conhecidas (Phase 4)

    - Allowlist exact-match dominio apenas (D-48). CIDR e wildcards (`*.example.com`) chegam em Phase 7.
    - Audit log e no-op (D-38). Hash-chained JSONL chega em Phase 8.
    - Sem TUI interativa. Aprovacao via flag `propose_only`. Keybindings TUI chegam em Phase 11.
    - TLDs nao-ICANN (`.br`, `.uk`) podem ter parse parcial (D-41). Ver `WhoisOutput.raw_unparsed`.
    ```

    PASSO 2 — CONTRIBUTING.md: APENDAR "## Adding a new TTP":

    ```markdown
    ## Adding a new TTP

    O kri0k abstrai TTPs atras de um trait async (`kri0k_core::ttp::Ttp`) e injeta o subprocess via `Subprocess` trait (ADR-0013). Pattern para adicionar nova TTP:

    ### 1. Implementar o struct + impl

    Em `crates/kri0k-core/src/ttp/<seu_ttp>.rs`:

    - Struct `MyTtp { subprocess: Arc<dyn Subprocess>, /* rate limit state */ }`
    - `#[async_trait] impl Ttp for MyTtp` com `id()`, `description()`, `rate_limits()`, `default_timeout()`, `async fn execute(target, cancel)`.

    Reuse WhoisTtp (`crates/kri0k-core/src/ttp/whois.rs`) como referencia. Sempre passar `cancel.clone()` ao subprocess.

    ### 2. Registrar no Engagement

    Em `crates/kri0k-pybridge/src/lib.rs`, dentro de `Engagement::new()`:

    ```rust
    registry.insert("MITRE.ID".to_string(), Box::new(MyTtp::new(subprocess.clone())));
    ```

    Quando o registry tiver >= 5 TTPs, refatorar para auto-discovery via crate `inventory` (deferido em D-52).

    ### 3. Capturar fixture + testes

    1. Rode o binario externo manualmente, capture stdout para `tests/fixtures/<ttp>_<target>.txt`.
    2. Duplique em `crates/kri0k-core/tests/fixtures/` (Pitfall 13 — path do cargo).
    3. Unit tests inline em `<seu_ttp>.rs` usando `MockSubprocess::from_fixture(...)`. Cobertura minima: parses_<target>, rate_limit_enforced, cancellation_returns_cancelled.
    4. Integration test gated por `#[cfg(feature = "integration")]` para o binary real.

    ### 4. Validacao

    ```bash
    cargo test --workspace                             # unit only (CI default)
    cargo test --workspace --features integration      # + binary real (local/nightly)
    cargo clippy --workspace --all-targets -- -D warnings
    ```

    ### 5. Documentar

    - CHANGELOG.md: entry sob proxima versao.
    - README.md "Running ..." section se aplicavel.
    - ADR novo se a TTP introduzir pattern arquitetural.
    ```

    PASSO 3 — Criar `CHANGELOG.md` (file novo se nao existir):

    ```markdown
    # Changelog

    Todas as mudancas notaveis ao projeto kri0k. Formato baseado em [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

    ## [0.2.0] - 2026-05-XX (Phase 4: Act + TTP Whois)

    ### Added
    - `kri0k_core::ttp::whois::WhoisTtp` — primeira TTP concreta (MITRE T1590.001).
    - `kri0k_core::ttp::subprocess::{Subprocess, RealSubprocess, MockSubprocess}` — abstracao para subprocess injection + testabilidade (ADR-0013).
    - `kri0k_core::scope::ScopeConfig` — parser de scope.yaml com schema v1 (lookahead) + allowlist exact-match (D-48).
    - `kri0k_core::audit::NoopAuditSink` — slot reservado para Phase 8 (rename de NoOpAuditSink).
    - `kri0k_pybridge::Engagement` — pyclass stateful expondo grafo + scope + TTP registry para Python (D-34).
    - `kri0k.engagement.create()` — Python bootstrap helper (D-36).
    - `kri0k_core::Error` variantes: ScopeViolation, RateLimitExceeded, SubprocessTimeout, ParseError, MissingDependency, UnknownTtp, Io, Cancelled.
    - NodeKind: Domain, Organization, Nameserver. EdgeKind: RegisteredBy, HasNameserver.
    - D-63 defense in depth: Layer 1 (scope allowlist), Layer 2 (regex domain validation pre-subprocess), Layer 3 (Command::arg sem shell).
    - Tracing instrumentation via `tracing` crate em hot paths.
    - Feature flag `integration` no kri0k-core para gating de tests com binary real.
    - Fixtures whois capturadas em tests/fixtures/.

    ### Changed
    - `kri0k_core::ttp::Ttp` trait agora e async (via `async-trait`). Trichotomia propose/dry-run/execute substituida por execute unico + propose_only gate em Python.
    - `kri0k_core::audit::NoOpAuditSink` renomeado para `NoopAuditSink` (D-38).
    - `python/kri0k/agent/nodes/act.py` reescrito: propose_only gate (D-49) + asyncio.to_thread dispatch (D-46) + history entry D-56.
    - `python/kri0k/agent/nodes/sense.py` modificado: prefere `engagement.snapshot()` quando disponivel; mantem fallback `_native.get_dummy_graph()` (backward-compat Phase 1/2).
    - `config/scope.example.yaml` reescrito para schema v1.

    ### Deferred (futuras phases)
    - Scope validation completa com CIDR + wildcards (Phase 7).
    - Audit log JSONL real com hash chain (Phase 8).
    - TUI interactive approval keybinding (Phase 11).
    - CLI commands (`kri0k init/run/status`) (Phase 12).
    - Auto-discovery de TTPs via `inventory` crate (quando >= 5 TTPs).
    - `pyo3-asyncio` para async pyclass methods (`asyncio.to_thread` suficiente).

    ### Security
    - M-02 (scope check pre-execution): partial — exact-match allowlist em Phase 4; CIDR + wildcards em Phase 7.
    - M-05 (propose-only default): covered — boolean flag default True.
    - M-15 (LLM nao executa direto): covered — facade Engagement minimal.
    - M-34 (TTP rate limit): covered — Mutex<Instant> por TTP.
    - M-36 (kill switch): covered — CancellationToken + fail-fast which::which em startup (wrapped em py.allow_threads per Pitfall 7).
    - AB-03 (prompt injection out-of-scope): covered — defense in depth D-63 (Layer 1 allowlist + Layer 2 regex domain validation + Layer 3 Command::arg sem shell).

    ## [0.1.0] - 2026-05-16 (Phases 1-3)
    - Phase 1: LangGraph structure base.
    - Phase 2: Sense node + Ollama provider.
    - Phase 3: Reason + Plan nodes.
    ```

    PASSO 4 — Criar `docs/adr/ADR-0013-ttp-trait-subprocess-abstraction.md`:

    ```markdown
    # ADR-0013: TTP trait async + Subprocess abstraction para testabilidade

    - **Status:** Accepted
    - **Data:** 2026-05-18
    - **Inputs:** Phase 4 implementacao (TTP-01..05), ADR-0012 (TTP trait skeleton sync), D-44, D-54, D-46

    ## Contexto

    ADR-0012 estabeleceu o trait `Ttp` como ponto de extensao para TTPs (M-15, M-34..M-36). Phase 4 implementou a primeira TTP concreta (T1590.001 whois) e precisou resolver dois pontos arquiteturais:

    1. **Async vs sync execute().** Subprocess via `std::process::Command` (sync) bloqueia o Tokio runtime do pybridge — incompativel com `tokio::time::timeout` e `CancellationToken`. Migracao para `tokio::process::Command` (async) e necessaria.
    2. **Testabilidade.** Unit tests com binary real sao lentos, flaky (rede), e exigem dependencia externa instalada. Tests devem rodar em CI greenfield sem `whois.exe`.

    ## Decisao

    1. **`Ttp` trait async via `async-trait`.** Metodo `async fn execute(&self, target: &str, cancel: CancellationToken) -> Result<TtpOutput, Error>`. Macro `#[async_trait]` torna o trait dyn-compatible (`Box<dyn Ttp>` para registry HashMap).

    2. **Abstracao `Subprocess` trait com Real + Mock impls.** TTP nao chama `tokio::process::Command` diretamente; injeta `Arc<dyn Subprocess>` no construtor:

    ```rust
    pub struct WhoisTtp {
        subprocess: Arc<dyn Subprocess>,
        last_call: Mutex<Option<Instant>>,
    }

    impl WhoisTtp {
        pub fn new(subprocess: Arc<dyn Subprocess>) -> Self { ... }
    }
    ```

    Implementacoes:
    - `RealSubprocess` — usa `tokio::process::Command` com `tokio::select! { biased; cancel, timeout, output }`.
    - `MockSubprocess::from_fixture(path)` — le fixture file, retorna SubprocessOutput sintetico.
    - `MockSubprocess::hanging()` — future que nunca completa, usado em timeout/cancel tests.

    3. **Cancel + timeout uniformes.** O `tokio::select!` na `RealSubprocess::run` cobre 3 branches com `biased` (cancel ganha):
        - `cancel.cancelled()` -> `start_kill() + wait()` + `Err(Cancelled)`.
        - `tokio::time::sleep(timeout)` -> `start_kill() + wait()` + `Err(SubprocessTimeout)`.
        - `child.wait_with_output()` -> Ok(SubprocessOutput).

    ## Consequencias

    - **+** Kill switch funcional (CancellationToken propaga, biased select garante prioridade).
    - **+** Timeout uniforme no nivel do Subprocess — TTPs nao reinventam.
    - **+** Unit tests rapidos via MockSubprocess (sem rede, sem binary).
    - **+** Adicionar TTP novo = 1 arquivo + 1 linha no registry do Engagement.
    - **-** Custo de 1 heap allocation por chamada async (preço do `#[async_trait]` para dyn-compat). Negligivel para 1 req/sec; revisitar se hot path.
    - **-** `Box<dyn Subprocess>` impede inline; tradeoff aceitavel.

    ## Alternativas consideradas

    - **Native `async fn` em trait (Rust 1.85 estavel):** rejeitado — NAO suporta `Box<dyn Trait>` (vtable nao pode armazenar diferentes `impl Future`). Registry HashMap<String, Box<dyn Ttp>> e requisito.
    - **`std::process::Command` sync direto na TTP:** rejeitado — bloqueia runtime, incompativel com cancellation_token + timeout via tokio::select.
    - **`pyo3-asyncio` para async pyclass methods:** deferido — `runtime().block_on` + `py.allow_threads` + Python side `asyncio.to_thread` e suficiente para Phase 4. Reavaliar quando ato chamar Python de dentro de async task.
    - **TTP-aware subprocess (sem trait abstraction):** rejeitado — cada TTP duplicaria timeout/cancel/kill_on_drop logic.

    ## Referencias

    - ADR-0001 (canonical state in Rust)
    - ADR-0012 (TTP trait skeleton — superceded para subprocess shape, complementa para risk_level)
    - `crates/kri0k-core/src/ttp/{mod,subprocess,whois}.rs`
    - RESEARCH.md Phase 4 secao "Pattern 1: tokio::process::Command + timeout + CancellationToken" + "Pattern 3: async-trait + Box<dyn Ttp> dispatch"
    ```

    Atualizar `docs/adr/README.md` (se existir um index) para listar ADR-0013. Se nao existir o index, criar uma linha simples no README.md raiz na lista de ADRs (se ja houver mencao).
  </action>
  <verify>
    <automated>grep -q "## Running the whois TTP" README.md && grep -q "## Adding a new TTP" CONTRIBUTING.md && grep -q "## \[0.2.0\]" CHANGELOG.md && ls docs/adr/ADR-0013-ttp-trait-subprocess-abstraction.md > /dev/null && grep -q "Trait + Subprocess abstraction\|TTP trait async + Subprocess abstraction" docs/adr/ADR-0013-ttp-trait-subprocess-abstraction.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: `README.md` contem string `## Running the whois TTP` E `winget install Microsoft.Sysinternals.Whois` (install hint).
    - Source: `README.md` contem snippet codigo Python com `from kri0k.engagement import create` E `await graph.ainvoke(state)`.
    - Source: `CONTRIBUTING.md` contem string `## Adding a new TTP` E `MockSubprocess::from_fixture` (test pattern).
    - Source: `CONTRIBUTING.md` contem comando `cargo test --features integration`.
    - Source: `CHANGELOG.md` existe E contem `## [0.2.0]` E lista de Added/Changed/Deferred/Security secoes.
    - Source: CHANGELOG menciona explicitamente `Engagement`, `WhoisTtp`, `ScopeConfig`, `NoopAuditSink rename`, `NodeKind: Domain, Organization, Nameserver`, `D-63 defense in depth: Layer 1 ... Layer 2 ... Layer 3`.
    - Source: `docs/adr/ADR-0013-ttp-trait-subprocess-abstraction.md` existe E comeca com `# ADR-0013:`.
    - Source: ADR-0013 secao "Decisao" referencia `async-trait`, `Subprocess trait`, `Real + Mock impls`, `tokio::select! { biased; ... }`.
    - Source: ADR-0013 secao "Alternativas consideradas" lista (a) native async fn (rejected), (b) std::process sync (rejected), (c) pyo3-asyncio (deferred), (d) TTP-aware subprocess (rejected).
    - Lint markdown: `markdownlint README.md CHANGELOG.md docs/adr/ADR-0013-*.md` exit code 0 se markdownlint disponivel; se ausente, skip silenciosamente.
  </acceptance_criteria>
  <done>4 arquivos de docs criados/atualizados per D-60. README quickstart para whois; CONTRIBUTING "Adding a new TTP"; CHANGELOG 0.2.0 com Added/Changed/Deferred/Security; ADR-0013 documenta o pattern Subprocess+async-trait.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python <-> Rust (PyO3) | Engagement methods sao a unica superficie; Python NAO muta grafo direto (D-35 facade) |
| LLM-generated proposal -> Engagement.execute_proposal | target str passa por D-63 Layer 2 regex (defense in depth) ANTES de scope.validate_target (Layer 1) antes do subprocess |
| Operator scope.yaml -> Engagement::new(scope_dict) | Operator-trusted; parser strict version=1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Elevation of Privilege | LLM dispara TTP sem aprovacao | mitigate | M-05 — D-49 propose_only flag default True em engagement.create() AND act.py honra. act early-returns sem chamar engagement.execute_proposal quando propose_only=True. Verified by test_act_propose_only_skips_rust (Task 4). Inline comment `# M-05: propose-only gate` em act.py. |
| T-04-05-02 | Information Disclosure | LLM injeta target out-of-scope; subprocess executa | mitigate | M-02 — Engagement.execute_proposal chama self.scope.validate_target(&target) ANTES do self.registry.get(&ttp_id) (fail-closed). Verified by test_scope_violation_short_circuits. Inline comment `// M-02 (D-48): fail-closed scope check` em pybridge lib.rs. |
| T-04-05-03 | Elevation of Privilege | LLM bypassa facade e muta grafo direto via Python | mitigate | M-15 — D-35 surface API minimal: 5 metodos (new/snapshot/execute_proposal/scope_hash/kill). NAO ha add_node / add_edge expostos. PyO3 #[pymethods] restringe acesso. |
| T-04-05-04 | Denial of Service | Operator perde controle; agente runaway | mitigate | M-36 — D-50 fail-fast em which::which("whois") em Engagement::new wrapped em py.allow_threads per Pitfall 7 (sem whois -> nao inicia, sem bloquear event loop). D-62 CancellationToken + Engagement.kill() expoe via PyO3. Verified Rust-side via `cargo test --package kri0k-pybridge --test engagement_missing_whois` (Task 4); test_kill_cancels_execute integration deferido (timing flaky). |
| T-04-05-05 | Tampering | scope.yaml editado mid-engagement | mitigate | M-03 — scope_hash computado uma vez em Engagement::new e exposto via scope_hash(). Phase 5+ pode re-check vs disk. |
| T-04-05-06 | Tampering / Prompt Injection | LLM proposal target shell metacharacters | mitigate | AB-03 (D-63 defense in depth) — **Layer 1**: scope.validate_target rejeita non-allowlist (M-02). **Layer 2**: regex domain validation (`DOMAIN_REGEX`) no execute_proposal pre-subprocess — COBERTO neste plano via `Regex::new(DOMAIN_REGEX).is_match(&target)`. Targets malformados retornam `Error::ParseError -> outcome.status == "error"` com mensagem "parse_error in proposal.target". Verified by test_invalid_target_rejected_by_regex (Task 4) + Rust unit test_domain_regex_accepts_valid_domains (Task 1). **Layer 3**: WhoisTtp passa target via Command::arg sem shell (Plano 04-03). Comment `// AB-03 D-63 L1+L2+L3 — full defense in depth` em pybridge. |
| T-04-05-07 | Repudiation | Sem audit de execucoes | mitigate (Phase 4 slot only) | M-22 — D-38 NoopAuditSink slot reservado. execute_proposal chama audit.log_ttp_execution() em cada execucao — vira no-op Phase 4, vira hash-chain Phase 8 sem mudar call sites. |
| T-04-05-08 | Race condition / Deadlock | Mutex<Graph> + Mutex<dedupe> lock order | mitigate | PATTERNS.md "Risk / landmine" lib.rs — apply_whois_output bloqueia dedupe ANTES de graph (lock order constante evita deadlock). Inline comment `// PATTERN: lock dedupe before graph to avoid deadlock` no source. |
| T-04-05-09 | Race condition | GIL + Tokio deadlock | mitigate | Pitfall 7 — `py.allow_threads(|| runtime().block_on(...))` em snapshot E execute_proposal E `#[new]` constructor (CONTEXT.md D-50 update — Pitfall 7 atinge tambem o construtor para evitar blocking event loop). NUNCA call back into Python de dentro de async task. Comment `// Pitfall 7: GIL released for async work` E `// Pitfall 7 (D-50 note): GIL released for blocking init` no source. |
| T-04-05-10 | Tampering / Crash | dedupe HashMap cresce sem limite em long-running engagement | accept | Phase 4 MVP single-engagement, short-lived; dedupe e bounded por |targets x kinds|. Documented como limitacao em CHANGELOG/CONTRIBUTING. Phase 5+ pode adicionar TTL. |

## Mitigations Mapped from CONTEXT.md D-61

| Mitigation | Status neste plano | Como o plano cobre |
|---|---|---|
| M-02 (scope violation pre-execution) | Covered (Phase 4 partial = exact-match) | Engagement.execute_proposal invoca scope.validate_target ANTES do registry.get (apos D-63 Layer 2 regex). test_scope_violation_short_circuits. |
| M-05 (propose-only default) | Covered | engagement.create(propose_only=True) default + act.py honra a flag. test_act_propose_only_skips_rust. |
| M-15 (LLM nao executa direto) | Covered | Engagement facade minimal (5 metodos); Python nao add_node/add_edge. |
| M-21 (TTP destrutivo) | N/A | Whois e RiskLevel::Safe; nao se aplica em Phase 4. |
| M-34 (TTP rate limit) | Covered | WhoisTtp tem Mutex<Instant> (Plano 04-03); Engagement nao bypass. |
| M-36 (kill switch) | Covered | D-50 fail-fast which() (wrapped em py.allow_threads per Pitfall 7) + D-62 CancellationToken + Engagement.kill(). Verified Rust-side em engagement_missing_whois.rs. |
| AB-03 (prompt injection) | **Covered (L1 + L2 + L3 — full defense in depth)** | L1 allowlist (M-02), **L2 regex domain validation no execute_proposal pre-subprocess (DOMAIN_REGEX constant + Regex::new + is_match check)**, L3 Command::arg sem shell (Plano 04-03). Verified by test_invalid_target_rejected_by_regex + test_domain_regex_accepts_valid_domains. |
</threat_model>

<verification>
- `cargo test --workspace` exit code 0 (todos os crates Phase 4 + pre-existentes verdes).
- `cargo test --workspace --features integration` exit code 0 (incluindo real_whois_smoke do Plano 04-03 + smoke tests Engagement do test_engagement_smoke.py).
- `cargo test --package kri0k-pybridge --test engagement_missing_whois` exit code 0 (D-50/M-36 Rust integration).
- `cargo clippy --workspace --all-targets --features integration -- -D warnings` exit code 0.
- `maturin develop --release` exit code 0 (constroi cdylib do pybridge e instala em venv).
- `pytest tests/ -m "not integration"` exit code 0 (8 act tests + tests pre-existentes Phase 1-3).
- `pytest tests/ -m integration` exit code 0 (skip se whois ausente; pass se presente — incluindo test_invalid_target_rejected_by_regex).
- `ruff check python/ tests/` exit code 0.
- `mypy python/kri0k --strict` exit code 0.
- `mypy tests/ --strict` exit code 0 (test files type-checked).
- Source: 4 arquivos de docs existem (README.md/CONTRIBUTING.md/CHANGELOG.md/ADR-0013).
- End-to-end smoke (se whois disponivel): `python -c "from kri0k.engagement import create; ctx = create({'version': 1, 'targets': ['example.com'], 'safeguards': {'propose_only': False}}, 'test'); print(ctx['engagement'].execute_proposal({'ttp_id': 'T1590.001', 'target': 'example.com'}))"` exibe outcome dict com status="executed" e graph_delta.nodes_added >= 1.
</verification>

<success_criteria>
- AGENT-05: act envia Proposal para Rust via Engagement.execute_proposal (via asyncio.to_thread), recebe outcome dict, anexa entry D-56 ao history. test_act_calls_execute_proposal_when_not_propose_only + test_act_outcome_appended_to_history.
- TTP-04 (completo): Engagement.execute_proposal aplica grafo deltas (Domain + Org + Nameservers per D-43 dedupe). test_whois_grows_graph integration confirma.
- ROADMAP Phase 4 criterios 1-4:
  1. TTP whois implementa trait Ttp em ttp/whois.rs (Plano 04-03 entregou; Plano 04-05 registra no Engagement).
  2. WhoisOutput retorna registrant/registrar/nameservers/dates (Plano 04-03 parser; verified test_whois_grows_graph snapshot).
  3. Grafo recebe Domain/Organization/Nameserver nodes (apply_whois_output Plano 04-05; test_whois_grows_graph).
  4. Rate limit 1 req/sec (Plano 04-03 WhoisTtp; respeitado em Engagement sem bypass).
- D-64 Definition of Done (todos 7 itens):
  1. ROADMAP 1-4 verificaveis -> sim.
  2. clippy strict zero warnings -> verified.
  3. ruff + mypy strict zero issues -> verified.
  4. pytest tests/ 100% + cargo test --features integration 100% -> verified.
  5. README + CONTRIBUTING + CHANGELOG + ADR-0013 entregues -> Task 5.
  6. Tabela M-XX em CONTEXT.md/VERIFICATION.md atualizada com status -> a fazer no SUMMARY do plano.
  7. End-to-end smoke `whois example.com` produz outcome status="executed" com registrant populado + grafo cresce -> manualmente em CHANGELOG; idealmente test_whois_grows_graph rodando em integration CI.
- D-63 defense in depth: **completo** com L1 + L2 + L3 (anteriormente L2 estava deferred — agora COBERTO neste plano).
- Pitfalls 1, 2, 5, 7 (incluindo construtor #[new]), 9, 11, 12 todos mitigados (verified em planos 04-01..04-05 com source assertions).
</success_criteria>

<output>
After completion, create `.planning/phases/04-act-node-ttp-whois/04-05-SUMMARY.md` registrando:
- Tamanho final do crates/kri0k-pybridge/src/lib.rs (LOC).
- Numero de tests Python (8 unit act + 8 integration smoke).
- Numero de tests Rust pybridge (8 unit inline + 2 integration em engagement_missing_whois.rs).
- Status do mypy strict + ruff (zero issues).
- Status do cargo test --features integration (whois disponivel? pass count).
- Decisao sobre M-XX coverage: M-02 covered (Phase 4 partial = exact-match), M-05/M-15/M-36 covered, M-34 covered, AB-03 **fully covered (L1+L2+L3)** — D-63 Layer 2 regex implementada neste plano (não mais deferred).
- Arquivos de docs criados (READ ME section heading, CONTRIBUTING section heading, CHANGELOG entry, ADR-0013 path).
- Confirmacao do D-64 Definition of Done — citar cada item dos 7.
- Atualizar `.planning/phases/04-act-node-ttp-whois/04-VALIDATION.md` frontmatter `nyquist_compliant: true` e `wave_0_complete: true` apos confirmar todos os tests verdes.
- Confirmacao que VALIDATION.md row D-50 aponta agora para `cargo test --package kri0k-pybridge --test engagement_missing_whois` (BLOCKER 4 fix).
- Lembrete: `/gsd-verify-work 4` para verificacao final + PR para master per D-59.
- Requisitos completados: AGENT-05, TTP-04 (Phase 4 closes TTP-01..05 completamente — todos os 6 requirements da Phase 4 cobertos por algum plano).
- Decisoes implementadas: D-34, D-35, D-36, D-38 (slot), D-43, D-46, D-47, D-49, D-50 (com Pitfall 7 nota), D-52, D-56, D-60, D-61 (table), D-62, D-63 (Layer 1+2+3).
</output>
</content>
</invoke>