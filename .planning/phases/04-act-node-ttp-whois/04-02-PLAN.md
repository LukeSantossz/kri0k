---
phase: 04-act-node-ttp-whois
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/kri0k-core/Cargo.toml
  - crates/kri0k-core/src/lib.rs
  - crates/kri0k-core/src/audit.rs
autonomous: true
requirements:
  - TTP-01
user_setup: []

must_haves:
  truths:
    - "kri0k_core::Error possui 8 novas variantes (ScopeViolation, RateLimitExceeded, SubprocessTimeout, ParseError, MissingDependency, UnknownTtp, Io, Cancelled) alem dos pre-existentes Json e Generic"
    - "Workspace declara deps tokio-util 0.7, which 8, async-trait 0.1, tracing 0.1, serde_yaml_ng 0.10, regex 1 com versoes exatas verificadas em RESEARCH.md (regex eh consumida pelo Plano 04-05 para D-63 Layer 2 domain validation)"
    - "kri0k-core::Cargo.toml ativa features tokio = [process, time, sync, macros] e declara feature flag `integration`"
    - "audit::NoOpAuditSink renomeado para NoopAuditSink (D-38), call sites internos atualizados"
    - "cargo check --workspace verde apos as mudancas (compilam standalone, ainda sem consumidores Phase 4)"
  artifacts:
    - path: "Cargo.toml"
      provides: "workspace.dependencies expanded com 5 novos crates"
      contains: "tokio-util ="
    - path: "crates/kri0k-core/Cargo.toml"
      provides: "kri0k-core deps + feature `integration` + dev-deps tokio + tracing-subscriber"
      contains: "[features]"
    - path: "crates/kri0k-core/src/lib.rs"
      provides: "Error enum expandido com 8 variantes novas"
      contains: "ScopeViolation"
    - path: "crates/kri0k-core/src/audit.rs"
      provides: "NoopAuditSink (renomeado)"
      contains: "pub struct NoopAuditSink"
  key_links:
    - from: "crates/kri0k-core/src/lib.rs"
      to: "crates/kri0k-core/src/ttp/{whois,subprocess}.rs (Plano 04-03)"
      via: "Plano 04-03 retorna `Error::ScopeViolation`, `Error::SubprocessTimeout`, `Error::Cancelled`, `Error::Io(...)` de WhoisTtp/Subprocess"
      pattern: "Error::(ScopeViolation|SubprocessTimeout|Cancelled|Io|RateLimitExceeded|ParseError|MissingDependency|UnknownTtp)"
    - from: "crates/kri0k-core/src/audit.rs"
      to: "crates/kri0k-pybridge/src/lib.rs (Plano 04-05)"
      via: "Engagement::new() instancia `Mutex<Box<dyn AuditSink>>` com NoopAuditSink default per D-38"
      pattern: "NoopAuditSink"
---

<objective>
Preparar a fundacao do crate `kri0k-core` para receber Phase 4: (1) declarar as 5 novas dependencias verificadas pelo RESEARCH.md no workspace, (2) ativar feature flags + dev-deps necessarias em `kri0k-core/Cargo.toml`, (3) expandir o enum `kri0k_core::Error` com as 8 variantes do D-53 + D-62 (`ScopeViolation`, `RateLimitExceeded`, `SubprocessTimeout`, `ParseError`, `MissingDependency`, `UnknownTtp`, `Io(#[from] std::io::Error)`, `Cancelled`), (4) renomear `NoOpAuditSink` -> `NoopAuditSink` (D-38).

Purpose: O Plano 04-03 (TTP module + WhoisTtp) e o Plano 04-04 (scope parser) consomem as novas variantes do Error e as deps. Sem este plano standalone na Wave 1, ambos quebrariam ao buildar. Paralelo com Plano 04-01 (kri0k-graph) — sem overlap de arquivos. Plano 04-05 consome o `NoopAuditSink` renomeado.

Output: 4 arquivos modificados; `cargo check --workspace` verde; `cargo test --package kri0k-core` continua verde (rename NoOpAuditSink -> NoopAuditSink so quebra call-sites internos do proprio arquivo, sem consumidores externos hoje).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-act-node-ttp-whois/04-CONTEXT.md
@.planning/phases/04-act-node-ttp-whois/04-RESEARCH.md
@.planning/phases/04-act-node-ttp-whois/04-PATTERNS.md
@Cargo.toml
@crates/kri0k-core/Cargo.toml
@crates/kri0k-core/src/lib.rs
@crates/kri0k-core/src/audit.rs

<interfaces>
<!-- Forma atual do Cargo.toml workspace (extraido de Cargo.toml:15-22) -->
```toml
[workspace.dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
tokio = { version = "1.40", features = ["rt", "rt-multi-thread", "macros"] }
pyo3 = { version = "0.24", features = ["extension-module"] }
petgraph = { version = "0.6", features = ["serde-1"] }
ulid = { version = "1.1", features = ["serde"] }
```

<!-- Forma atual do Error enum (extraido de crates/kri0k-core/src/lib.rs:17-29) -->
```rust
#[derive(Debug, thiserror::Error)]
pub enum Error {
    #[error("JSON error: {0}")]
    Json(#[from] serde_json::Error),
    #[error("{0}")]
    Generic(String),
}
pub type Result<T> = std::result::Result<T, Error>;
```

<!-- Versoes exatas verificadas em RESEARCH.md "Standard Stack" (linhas 150-170): -->
- tokio-util = "0.7.18", features ["rt"] (D-62 CancellationToken; MSRV 1.71)
- which = "8.0.2" (D-50 binary detection; MSRV 1.70)
- async-trait = "0.1.89" (D-44 dyn-compat async trait; MSRV 1.56)
- tracing = "0.1.44" (D-55 instrumentation; MSRV 1.65)
- serde_yaml_ng = "0.10.0" (D-58 YAML parser — NUNCA serde_yaml/serde_yml per Pitfall 9)

<!-- NoOpAuditSink existente (extraido de crates/kri0k-core/src/audit.rs:83-108) — todos call-sites internos -->
- linha 87: `pub struct NoOpAuditSink;`
- linha 89: `impl AuditSink for NoOpAuditSink {`
- linha 118: `Ok(Box::new(NoOpAuditSink))` dentro de `create_audit_sink`
- linha 86: comentario `/// TODO(T7/M-13): Replace with real append-only implementation.` — manter como esta
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar deps no workspace Cargo.toml e no kri0k-core/Cargo.toml</name>
  <files>Cargo.toml, crates/kri0k-core/Cargo.toml</files>
  <read_first>
    - Cargo.toml (forma atual do bloco [workspace.dependencies] linhas 15-22)
    - crates/kri0k-core/Cargo.toml (forma atual completa)
    - .planning/phases/04-act-node-ttp-whois/04-RESEARCH.md secao "Standard Stack" e "Installation Plan"
    - .planning/phases/04-act-node-ttp-whois/04-PATTERNS.md "Risk / landmine" da subsection scope.rs (sobre sha2 transitivo)
  </read_first>
  <action>
    Editar `Cargo.toml` (workspace root) — APENDAR ao bloco `[workspace.dependencies]` (apos linha 22, antes do `[workspace.lints.rust]`):

    ```
    tokio-util = { version = "0.7", features = ["rt"] }
    which = "8"
    async-trait = "0.1"
    tracing = "0.1"
    serde_yaml_ng = "0.10"
    regex = "1"
    ```

    NAO usar `serde_yaml` (deprecated 2024-03-25, Pitfall 9). NAO usar `serde_yml` (RUSTSEC-2025-0068, Pitfall 9). NAO mover ou alterar linhas pre-existentes (serde/serde_json/thiserror/tokio/pyo3/petgraph/ulid).

    NAO alterar o bloco `[workspace.lints.*]` (linhas 24-53) — strict lints (`unwrap_used = "deny"`, `panic = "deny"`, `unimplemented = "deny"`) permanecem aplicaveis ao codigo Phase 4.

    Editar `crates/kri0k-core/Cargo.toml` — substituir o bloco `[dependencies]` atual (linhas 11-15) por:

    ```
    [dependencies]
    serde.workspace = true
    serde_json.workspace = true
    serde_yaml_ng.workspace = true
    thiserror.workspace = true
    ulid.workspace = true
    tokio = { workspace = true, features = ["process", "time", "sync", "macros"] }
    tokio-util.workspace = true
    which.workspace = true
    async-trait.workspace = true
    tracing.workspace = true
    sha2 = "0.10"
    ```

    Sobre `sha2 = "0.10"`: D-58 / Plano 04-04 usa SHA-256 para `ScopeConfig::compute_hash`. Antes de adicionar, executar `cargo tree --package kri0k-core | findstr sha2` (PowerShell) ou `cargo tree --package kri0k-core | grep sha2` (bash). Se sha2 ja existir transitivamente via ulid, ainda adicionar como dependencia direta (declarativo) para nao depender de cadeia transitiva.

    Apendar ao final do arquivo `crates/kri0k-core/Cargo.toml`:

    ```
    [features]
    default = []
    integration = []

    [dev-dependencies]
    tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
    tracing-subscriber = "0.3"
    ```

    Feature `integration` e gating de testes que executam whois.exe real (D-54, D-57). `tracing-subscriber` em dev-deps so para tests que precisam observar spans (Pitfall 10).
  </action>
  <verify>
    <automated>cargo check --workspace</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: grep `tokio-util = { version = "0.7"` em `Cargo.toml` retorna >= 1 match.
    - Source assertion: grep `which = "8"` em `Cargo.toml` retorna >= 1 match.
    - Source assertion: grep `serde_yaml_ng = "0.10"` em `Cargo.toml` retorna >= 1 match.
    - Source assertion: grep `async-trait = "0.1"` em `Cargo.toml` retorna >= 1 match.
    - Source assertion: grep `tracing = "0.1"` em `Cargo.toml` retorna >= 1 match.
    - Source assertion: grep `regex = "1"` em `Cargo.toml` retorna >= 1 match (D-63 Layer 2 dep para Plano 04-05).
    - Source assertion: grep `serde_yaml` em `Cargo.toml` retorna ZERO matches alem de `serde_yaml_ng` (i.e., grep `serde_yaml = ` exato retorna 0). Filtro: `grep -E "^serde_yaml = " Cargo.toml | wc -l` == 0.
    - Source assertion: grep `serde_yml` em `Cargo.toml` retorna ZERO matches (proibido pela Pitfall 9).
    - Source assertion: grep `\[features\]` em `crates/kri0k-core/Cargo.toml` retorna >= 1 match.
    - Source assertion: grep `integration = \[\]` em `crates/kri0k-core/Cargo.toml` retorna >= 1 match.
    - Behavior: `cargo check --workspace` exit code 0 (nao quebrou nada — as deps so foram declaradas, ainda nao usadas; sha2 pode dar warning de unused mas nao deve falhar).
    - Behavior: `cargo check --workspace --features integration` exit code 0 (feature flag valida).
  </acceptance_criteria>
  <done>5 deps novas no workspace, kri0k-core declara todas + sha2 + feature flag integration + dev-deps tokio e tracing-subscriber. cargo check verde no workspace, incluindo com --features integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expandir Error enum + renomear NoOpAuditSink -> NoopAuditSink</name>
  <files>crates/kri0k-core/src/lib.rs, crates/kri0k-core/src/audit.rs</files>
  <read_first>
    - crates/kri0k-core/src/lib.rs (forma atual do Error enum nas linhas 17-29)
    - crates/kri0k-core/src/audit.rs (todas as 3 ocorrencias de NoOpAuditSink: linhas 87, 89, 118)
    - .planning/phases/04-act-node-ttp-whois/04-CONTEXT.md (D-53 detalhe de cada variante; D-62 Cancelled; D-38 NoopAuditSink)
    - .planning/phases/04-act-node-ttp-whois/04-PATTERNS.md "Risk / landmine" da subsection lib.rs (Io variante ordering)
  </read_first>
  <behavior>
    - Test: `Error::ScopeViolation { target: "evil.com".into(), reason: "not in allowlist".into() }` formata via Display como string nao-vazia contendo "evil.com"
    - Test: `Error::from(std::io::Error::new(std::io::ErrorKind::NotFound, "x"))` produz `Error::Io(_)` (graças ao `#[from]`)
    - Test: `Error::MissingDependency { binary: "whois".into() }` formata mensagem mencionando "whois"
    - Test: `Error::Cancelled` Display retorna string nao-vazia (sem placeholders)
    - Test: `Error::Json(_)` continua funcionando (`#[from] serde_json::Error`) — regressao
    - Test: `audit::NoopAuditSink::default()` cria uma instancia; `Box::new(NoopAuditSink::default()) as Box<dyn AuditSink>` compila
  </behavior>
  <action>
    Editar `crates/kri0k-core/src/lib.rs` — expandir o enum `Error` (linhas 17-26) para conter as 8 novas variantes APOS as 2 existentes (`Json`, `Generic` ficam primeiro; Cancelled vai POR ULTIMO conforme convenção PATTERNS.md "Risk / landmine"):

    Forma final do enum (rustdoc + Display string por variante):

    - `Json(#[from] serde_json::Error)` — manter como esta
    - `Generic(String)` — manter como esta
    - `Io(#[from] std::io::Error)` — `#[error("I/O error: {0}")]`. SAFE: nenhum outro variant ja usa `#[from] std::io::Error` (verificado nas linhas atuais).
    - `ScopeViolation { target: String, reason: String }` — `#[error("scope violation for target {target:?}: {reason}")]`. M-02 + M-15: usado pelo Engagement antes de qualquer subprocess.
    - `RateLimitExceeded { ttp_id: String, retry_in_ms: u64 }` — `#[error("rate limit exceeded for TTP {ttp_id}: retry in {retry_in_ms}ms")]`. M-34.
    - `SubprocessTimeout { ttp_id: String, timeout_ms: u64 }` — `#[error("TTP {ttp_id} subprocess timeout after {timeout_ms}ms")]`. M-34 (subprocess hard-limit).
    - `ParseError { source: String, detail: String }` — `#[error("parse error in {source}: {detail}")]`. Usado por Plano 04-04 (scope.yaml) e parse de WhoisOutput (heuristic; D-41 mantem total mas reservar a variante).
    - `MissingDependency { binary: String }` — `#[error("missing dependency: binary {binary:?} not found in PATH. Install with: winget install Microsoft.Sysinternals.Whois (Windows) or apt install whois (Linux)")]`. D-50 fail-fast + hint amigavel per `<specifics>` do CONTEXT.md.
    - `UnknownTtp { ttp_id: String }` — `#[error("unknown TTP id: {ttp_id:?}")]`.
    - `Cancelled` — `#[error("operation cancelled")]`. D-62 — kill switch.

    Cada variante recebe rustdoc `///` curto (1 linha) acima do `#[error(...)]` — pattern do projeto exige `missing_docs = "warn"` (workspace lint). Exemplo: `/// Scope violation: target not in scope allowlist (M-02).` Importante: NAO usar `#[from]` em variantes NamedField (D-53 nao especifica From manuais; apenas `Json` e `Io` tem `#[from]`).

    NAO mudar `pub type Result<T> = std::result::Result<T, Error>;` (linha 29).
    NAO mudar declaracoes `pub mod audit;`, `pub mod safeguards;`, `pub mod scope;`, `pub mod ttp;` (linhas 7-14) — Plano 04-03 vai converter `ttp` de file para diretorio, Rust resolve automaticamente.

    Adicionar dentro de `#[cfg(test)] mod tests` (linhas 91-117), apos os testes existentes:
    - `test_error_io_from_conversion` — cria `std::io::Error::new(...)`, faz `let e: Error = io_err.into();`, assert `matches!(e, Error::Io(_))`.
    - `test_error_scope_violation_display` — cria `Error::ScopeViolation { target: "evil.com".into(), reason: "not allowed".into() }`, assert `e.to_string().contains("evil.com")`.
    - `test_error_missing_dependency_display` — cria `Error::MissingDependency { binary: "whois".into() }`, assert `e.to_string().contains("whois")` e `.contains("PATH")`.
    - `test_error_cancelled_display` — `assert_eq!(Error::Cancelled.to_string(), "operation cancelled")`.
    - `test_error_json_still_works` (regressao) — `Error::from(serde_json::from_str::<i32>("not json").unwrap_err())` deve dar `Error::Json(_)`.

    Editar `crates/kri0k-core/src/audit.rs`:
    - Linha 86 (comentario): manter `/// No-op audit sink for testing.` mas atualizar TODO ref se necessario (manter como esta).
    - Linha 87: renomear `pub struct NoOpAuditSink;` -> `pub struct NoopAuditSink;`.
    - Linha 89: renomear `impl AuditSink for NoOpAuditSink {` -> `impl AuditSink for NoopAuditSink {`.
    - Linha 118: dentro de `create_audit_sink`, renomear `Ok(Box::new(NoOpAuditSink))` -> `Ok(Box::new(NoopAuditSink))`.

    Verificar via grep que nao ha outros call-sites de `NoOpAuditSink` no projeto antes de aceitar a mudanca. Pattern check: rodar `grep -r "NoOpAuditSink" --include "*.rs"` em todo o repo apos a edicao — deve retornar 0 matches.

    Adicionar 1 teste novo em `crates/kri0k-core/src/audit.rs` dentro de um `#[cfg(test)] mod tests` (criar o bloco se nao existir — o arquivo atual nao tem):
    - `test_noop_audit_sink_is_boxable` — `let _sink: Box<dyn AuditSink> = Box::new(NoopAuditSink::default());`; chamar `_sink.log_ttp_execution(TtpExecutionEvent { timestamp: "2026-01-01T00:00:00Z".into(), ttp_id: "test".into(), target: "x".into(), outcome: "ok".into(), llm_provider: None })` e assert `is_ok()`. Confirma que o rename nao quebrou o trait impl.
    - `test_noop_audit_sink_is_mutex_boxable_sync` (WARNING 2 — invariant Pitfall 5 + 12) — compile-time assertion que `Mutex<Box<dyn AuditSink + Send>>` e Sync (requisito do PyO3 0.24 pyclass do Plano 04-05):
      ```
      #[test]
      fn test_noop_audit_sink_is_mutex_boxable_sync() {
          // Confirms Pitfall 5 + Pitfall 12 invariant: Box<dyn AuditSink + Send> dentro de Mutex e Sync.
          // Plano 04-05 Engagement::audit usa exatamente este shape — falha aqui = quebra o pyclass.
          let _: std::sync::Mutex<Box<dyn AuditSink + Send>> =
              std::sync::Mutex::new(Box::new(NoopAuditSink::default()));
          fn assert_sync<T: Sync>() {}
          assert_sync::<std::sync::Mutex<Box<dyn AuditSink + Send>>>();
      }
      ```
      Este teste eh compile-time (assert_sync forca trait bound check) + runtime (instancia Mutex). NOTA: se `AuditSink` trait nao declara `Send` como supertrait no Plano 02-pre-existente, este test forca pensar — checar `crates/kri0k-core/src/audit.rs` original; se necessario adicionar `pub trait AuditSink: Send + Sync` para satisfazer PyO3 Sync requirement (Pitfall 12). Documentar como pre-requisito do Plano 04-05.
  </action>
  <verify>
    <automated>cargo test --package kri0k-core</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `crates/kri0k-core/src/lib.rs` contains literal substring `ScopeViolation {` AND `RateLimitExceeded {` AND `SubprocessTimeout {` AND `ParseError {` AND `MissingDependency {` AND `UnknownTtp {` AND `Io(` AND `Cancelled` dentro do bloco `pub enum Error`.
    - Source assertion: `Json(#[from] serde_json::Error)` e `Generic(String)` continuam presentes (regressao D-53 "Existing Json e Generic permanecem").
    - Source assertion: `Io(#[from] std::io::Error)` — exatamente UM `#[from]` para `std::io::Error` (sem duplicacao).
    - Source assertion: `crates/kri0k-core/src/audit.rs` contains `pub struct NoopAuditSink;` (com minusculo o) — grep `NoopAuditSink` retorna >= 3 matches (struct + impl + create_audit_sink).
    - Source assertion: `grep -r "NoOpAuditSink" crates/ --include "*.rs"` retorna ZERO matches (rename complete).
    - Behavior: `cargo test --package kri0k-core` exit code 0; output exibe novos testes `test_error_io_from_conversion`, `test_error_scope_violation_display`, `test_error_missing_dependency_display`, `test_error_cancelled_display`, `test_error_json_still_works`, `test_noop_audit_sink_is_boxable`, `test_noop_audit_sink_is_mutex_boxable_sync`, todos como `ok`.
    - Behavior: testes pre-existentes `test_node_id_uniqueness`, `test_edge_id_uniqueness`, `test_node_id_serialization` continuam `ok` (regressao Phase 1).
    - Lint: `cargo clippy --package kri0k-core --all-targets -- -D warnings` exit code 0 (Display strings nao acionam warnings; rustdoc obrigatorio satisfeito pelo `/// ...` de cada variante).
  </acceptance_criteria>
  <done>Error tem 10 variantes (2 + 8). NoOpAuditSink renomeado em 3 locais sem call-sites residuais. 7 novos testes verdes (6 originais + 1 Sync invariant) + regressao Phase 1 intacta. Clippy strict OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust internal (sem cross-language neste plano) | Sem fronteira de confianca — extensao de tipos + dep declaration only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering / Supply Chain | Dependency on serde_yaml (deprecated) or serde_yml (RUSTSEC-2025-0068) | mitigate | Plano forca `serde_yaml_ng = "0.10"` per Pitfall 9. Acceptance criteria grep verifica ausencia das alternativas. RESEARCH.md cita advisory URL: rustsec.org/advisories/RUSTSEC-2025-0068.html |
| T-04-02-02 | Information Disclosure | Error message vazando paths/secrets | accept | Error variants usam strings tipadas controladas (target, ttp_id, binary name, reason). Sem stack traces auto-injetadas. `Generic(String)` (legacy) ja existia — escopo do plano nao expande sua superficie. |

## Mitigations Mapped from CONTEXT.md D-61

| Mitigation | Status neste plano | Como o plano cobre |
|---|---|---|
| M-36 (kill switch) | Partial — variant only | `Error::Cancelled` declarada (consumida no Plano 04-03 via CancellationToken). |
| M-02 (scope violation) | Partial — variant only | `Error::ScopeViolation { target, reason }` declarada (consumida no Plano 04-04 + 04-05). |
| M-34 (TTP rate limit) | Partial — variants only | `Error::RateLimitExceeded` e `Error::SubprocessTimeout` declaradas (Plano 04-03 emite). |
</threat_model>

<verification>
- `cargo check --workspace` exit code 0.
- `cargo check --workspace --features integration` exit code 0.
- `cargo test --package kri0k-core` exit code 0 (3 existentes + 7 novos = 10 testes).
- `cargo clippy --package kri0k-core --all-targets -- -D warnings` exit code 0.
- Source: `grep -r "NoOpAuditSink" crates/ --include "*.rs" | wc -l` == 0.
- Source: `grep -E "^serde_yaml = " Cargo.toml | wc -l` == 0 (filtra `serde_yaml_ng` que comeca com `serde_yaml_ng = `).
</verification>

<success_criteria>
- Deps verificadas em RESEARCH.md presentes no workspace e propagadas para kri0k-core.
- Feature `integration` definida para gating de tests Phase 4 (Plano 04-03).
- `Error` enum cobre o vocabulario completo para Phase 4: scope, rate limit, timeout, parse, missing dep, unknown TTP, IO, cancelled.
- Rename NoOpAuditSink -> NoopAuditSink (D-38) executado sem deixar residuos.
- TTP-01 (parcial — pre-requisito Error/deps): satisfied for Plano 04-03 consumir.
</success_criteria>

<output>
After completion, create `.planning/phases/04-act-node-ttp-whois/04-02-SUMMARY.md` registrando:
- Lista de deps adicionadas com versoes
- Lista de Error variants adicionadas (8 novas)
- Confirmacao do rename NoOpAuditSink (3 call sites alterados)
- Total de testes no kri0k-core depois do plano
- Status do cargo check workspace + feature integration
- Requisito coberto: TTP-01 (parcial — pre-requisito; consumido no Plano 04-03)
</output>
