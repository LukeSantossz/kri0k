---
phase: 04-act-node-ttp-whois
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/kri0k-graph/src/lib.rs
autonomous: true
requirements:
  - TTP-04
user_setup: []

must_haves:
  truths:
    - "NodeKind serializa as variantes Domain / Organization / Nameserver com snake_case JSON"
    - "EdgeKind serializa RegisteredBy e HasNameserver como tagged unit variants"
    - "Variantes pre-existentes (Host, Network, Service, Finding, BelongsTo, RunsOn, RelatesTo) continuam funcionando"
  artifacts:
    - path: "crates/kri0k-graph/src/lib.rs"
      provides: "NodeKind { Domain, Organization, Nameserver } + EdgeKind { RegisteredBy, HasNameserver }"
      contains: "pub enum NodeKind"
  key_links:
    - from: "crates/kri0k-graph/src/lib.rs"
      to: "crates/kri0k-pybridge/src/lib.rs (Plano 04-05)"
      via: "Engagement.apply_whois_output consome NodeKind::Domain, Organization, Nameserver e EdgeKind::RegisteredBy, HasNameserver"
      pattern: "NodeKind::(Domain|Organization|Nameserver)"
---

<objective>
Expandir o data model do grafo (`crates/kri0k-graph`) com as variantes necessarias para o dominio whois: `NodeKind::Domain { name }`, `NodeKind::Organization { name }`, `NodeKind::Nameserver { hostname }`, `EdgeKind::RegisteredBy` e `EdgeKind::HasNameserver`. Esta extensao e a fundacao consumida pelo Plano 04-05 (Engagement pyclass).

Purpose: Sem essas variantes o `Engagement::apply_whois_output` nao consegue inserir nos tipados — TTP-04 exige Domain / Organization / Nameserver nodes (per D-40) e edges RegisteredBy / HasNameserver (per D-39). Plano executavel sem dependencia de outros planos da Phase 4 (compilado standalone, paralelo com 04-02).

Output: `crates/kri0k-graph/src/lib.rs` modificado em-place; cargo build/test do crate continua verde; tests Phase 1 (test_smoke.py com host/network/service) nao quebram porque sao adicoes nao-refatoradoras.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-act-node-ttp-whois/04-CONTEXT.md
@.planning/phases/04-act-node-ttp-whois/04-PATTERNS.md
@crates/kri0k-graph/src/lib.rs

<interfaces>
<!-- Forma atual do enum (a estender, NAO refatorar). Extraido de crates/kri0k-graph/src/lib.rs:9-49 -->

```rust
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(tag = "type", rename_all = "snake_case")]
pub enum NodeKind {
    Host { ip: String },
    Network { cidr: String },
    Service { port: u16, protocol: String },
    Finding { description: String },
}

#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(tag = "type", rename_all = "snake_case")]
pub enum EdgeKind {
    BelongsTo,
    RunsOn,
    RelatesTo { relation: String },
}
```

Atributos serde existentes (`tag = "type"`, `rename_all = "snake_case"`) DEVEM ser preservados. Novas variantes serializam como:
- NodeKind::Domain -> `{"type": "domain", "name": "..."}`
- NodeKind::Organization -> `{"type": "organization", "name": "..."}`
- NodeKind::Nameserver -> `{"type": "nameserver", "hostname": "..."}`
- EdgeKind::RegisteredBy -> `{"type": "registered_by"}`
- EdgeKind::HasNameserver -> `{"type": "has_nameserver"}`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender NodeKind com Domain, Organization, Nameserver</name>
  <files>crates/kri0k-graph/src/lib.rs</files>
  <read_first>
    - crates/kri0k-graph/src/lib.rs (forma atual do enum NodeKind nas linhas 9-34)
    - .planning/phases/04-act-node-ttp-whois/04-CONTEXT.md (D-40: natural keys + metadata layout)
    - .planning/phases/04-act-node-ttp-whois/04-PATTERNS.md (secao "crates/kri0k-graph/src/lib.rs (extend NodeKind + EdgeKind enums)")
  </read_first>
  <behavior>
    - Test: deserializar `{"type": "domain", "name": "example.com"}` produz `NodeKind::Domain { name: "example.com".to_string() }`
    - Test: deserializar `{"type": "organization", "name": "Example Inc."}` produz `NodeKind::Organization { name: "Example Inc.".to_string() }`
    - Test: deserializar `{"type": "nameserver", "hostname": "ns1.example.com"}` produz `NodeKind::Nameserver { hostname: "ns1.example.com".to_string() }`
    - Test: serializar `NodeKind::Domain { name: "x".into() }` produz JSON com `"type":"domain","name":"x"`
    - Test (regressao): `NodeKind::Host { ip: "1.2.3.4".into() }` continua serializando como `"type":"host","ip":"1.2.3.4"`
  </behavior>
  <action>
    Estender `pub enum NodeKind` em `crates/kri0k-graph/src/lib.rs` adicionando 3 novas variantes APOS `Finding` (mantendo ordem semantica: existentes primeiro, novas por ultimo, conforme D-40):

    - `Domain { name: String }` com rustdoc `/// Domain name (e.g., "example.com") — natural key for whois targets.`
    - `Organization { name: String }` com rustdoc `/// Organization name extracted from whois Registrant Organization (D-42).`
    - `Nameserver { hostname: String }` com rustdoc `/// Nameserver hostname (e.g., "ns1.example.com") — case-insensitive natural key.`

    NAO alterar derive macros (`#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]`) nem o atributo `#[serde(tag = "type", rename_all = "snake_case")]` — eles sao reusados pelas novas variantes.

    NAO mover/renomear/remover variantes existentes (Host, Network, Service, Finding) — D-39/D-40 sao adicoes, nao refatoramento. Quebra de tests Phase 1 (tests/test_smoke.py linhas 51-58) e bloqueante.

    Adicionar dentro de `#[cfg(test)] mod tests` (linhas 199-260):
    - `test_node_kind_domain_serialization` — cria `NodeKind::Domain { name: "example.com".into() }`, serializa via `serde_json::to_value`, assert que o JSON e `{"type":"domain","name":"example.com"}`.
    - `test_node_kind_organization_serialization` — analogo para `Organization { name }`.
    - `test_node_kind_nameserver_serialization` — analogo para `Nameserver { hostname }`.
    - `test_node_kind_deserialization_roundtrip` — para cada uma das 3 novas variantes, faz `serde_json::from_value(serde_json::to_value(&kind).unwrap()).unwrap()` e assert `==` com a original.
    - `test_node_kind_existing_variants_still_work` (regressao) — assert que `NodeKind::Host { ip: "1.2.3.4".into() }` serializa como `{"type":"host","ip":"1.2.3.4"}` (M-XX nao aplica aqui — sao tests).

    Tests devem usar `.expect("serialize")` (consistencia com tests existentes nas linhas 248-258) — `expect_used` ja tem `#[allow(clippy::expect_used)]` global em testes nas linhas 199-200.
  </action>
  <verify>
    <automated>cargo test --package kri0k-graph node_kind</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `crates/kri0k-graph/src/lib.rs` contains literal substring `Domain {` AND `Organization {` AND `Nameserver {` dentro do bloco `pub enum NodeKind`.
    - Source assertion: grep `pub enum NodeKind` em `crates/kri0k-graph/src/lib.rs` retorna exatamente 1 match (nao duplicou enum).
    - Source assertion: `Host {`, `Network {`, `Service {`, `Finding {` continuam presentes no mesmo enum (regressao Phase 1).
    - Behavior: `cargo test --package kri0k-graph node_kind` retorna exit code 0 e exibe `test test_node_kind_domain_serialization ... ok`, `test test_node_kind_organization_serialization ... ok`, `test test_node_kind_nameserver_serialization ... ok`, `test test_node_kind_deserialization_roundtrip ... ok`, `test test_node_kind_existing_variants_still_work ... ok` (5 testes new + os 4 existentes).
    - Lint: `cargo clippy --package kri0k-graph --all-targets -- -D warnings` exit code 0.
  </acceptance_criteria>
  <done>NodeKind possui 7 variantes (4 existentes + 3 novas). Todos os 5 testes do enum passam. Clippy strict verde no crate kri0k-graph.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estender EdgeKind com RegisteredBy e HasNameserver</name>
  <files>crates/kri0k-graph/src/lib.rs</files>
  <read_first>
    - crates/kri0k-graph/src/lib.rs (forma atual do enum EdgeKind nas linhas 36-49)
    - .planning/phases/04-act-node-ttp-whois/04-CONTEXT.md (D-39: variantes specificas vs RelatesTo)
    - .planning/phases/04-act-node-ttp-whois/04-PATTERNS.md (secao "crates/kri0k-graph/src/lib.rs (extend NodeKind + EdgeKind enums)")
  </read_first>
  <behavior>
    - Test: serializar `EdgeKind::RegisteredBy` produz JSON `{"type":"registered_by"}` (sem campos)
    - Test: serializar `EdgeKind::HasNameserver` produz JSON `{"type":"has_nameserver"}`
    - Test: deserializar `{"type":"registered_by"}` produz `EdgeKind::RegisteredBy`
    - Test: deserializar `{"type":"has_nameserver"}` produz `EdgeKind::HasNameserver`
    - Test (regressao): `EdgeKind::BelongsTo` continua serializando como `{"type":"belongs_to"}`
  </behavior>
  <action>
    Estender `pub enum EdgeKind` em `crates/kri0k-graph/src/lib.rs` adicionando 2 novas variantes APOS `RelatesTo` (manter ordem):

    - `RegisteredBy` (unit variant, sem campos) com rustdoc `/// Domain -> Organization edge: domain is registered by this organization (D-39).`
    - `HasNameserver` (unit variant, sem campos) com rustdoc `/// Domain -> Nameserver edge: domain delegates DNS to this nameserver (D-39).`

    Padrao identico a `BelongsTo` e `RunsOn` existentes (unit variants). NAO usar struct-like ou tuple-like — D-39 especifica que metadata (e.g., timestamp da relacao) vai em `Edge.metadata` futuramente (Phase 5+), nao no payload da variante.

    Manter `RelatesTo { relation: String }` — D-39 diz "permanece para casos exploratorios". NAO remover.

    Adicionar dentro de `#[cfg(test)] mod tests`:
    - `test_edge_kind_registered_by_serialization` — `serde_json::to_value(&EdgeKind::RegisteredBy).unwrap()` igual a `serde_json::json!({"type":"registered_by"})`.
    - `test_edge_kind_has_nameserver_serialization` — analogo para HasNameserver.
    - `test_edge_kind_new_variants_roundtrip` — para cada nova variante, `from_value(to_value(&kind)) == kind`.
    - `test_edge_kind_existing_variants_still_work` (regressao) — assert que `EdgeKind::BelongsTo` serializa como `{"type":"belongs_to"}`.
  </action>
  <verify>
    <automated>cargo test --package kri0k-graph edge_kind</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: grep `RegisteredBy` em `crates/kri0k-graph/src/lib.rs` retorna >= 1 match dentro do bloco `pub enum EdgeKind`.
    - Source assertion: grep `HasNameserver` em `crates/kri0k-graph/src/lib.rs` retorna >= 1 match dentro do bloco `pub enum EdgeKind`.
    - Source assertion: `RelatesTo {` continua presente (D-39 nao remove).
    - Behavior: `cargo test --package kri0k-graph edge_kind` exit code 0, exibe 4 novos testes verdes.
    - Lint: `cargo clippy --package kri0k-graph --all-targets -- -D warnings` exit code 0.
    - Regressao: `cargo test --package kri0k-graph` exit code 0 (testes Phase 1 do Graph::add_edge continuam passando).
  </acceptance_criteria>
  <done>EdgeKind possui 5 variantes (3 existentes + 2 novas). Todos os testes do crate kri0k-graph passam. Clippy strict verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rust internal (sem cross-language neste plano) | Nao ha I/O externo nem fronteira de confianca — somente extensao de enum |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | NodeKind serialization JSON | accept | Variantes sao tagged via `#[serde(tag = "type", rename_all = "snake_case")]` — formato determinístico evita JSON ambiguous parsing. Risco residual baixo: payload da variante e tipado (`String`), `serde` rejeita formatos invalidos em deserialize. Sem cross-language input neste plano (Engagement::apply_whois_output em Plano 04-05 e o produtor). |

## Mitigations Mapped from CONTEXT.md D-61

Nenhuma mitigation M-XX direta neste plano (data model only). As mitigations consumidoras (M-15 via Engagement) sao implementadas no Plano 04-05.
</threat_model>

<verification>
- `cargo test --package kri0k-graph` exit code 0 (todos os 4 testes existentes + 9 novos: 5 NodeKind + 4 EdgeKind = 13 total).
- `cargo clippy --package kri0k-graph --all-targets -- -D warnings` exit code 0.
- `cargo check --workspace` exit code 0 (nao quebrou nenhum consumidor downstream — Phase 4 ainda nao tem consumidores, mas check garante que `kri0k_native` continua compilando).
- Source assertion: 5 novas variantes (3 NodeKind + 2 EdgeKind) presentes nos enums.
</verification>

<success_criteria>
- TTP-04 (data model parcial): NodeKind::Domain / Organization / Nameserver existem; EdgeKind::RegisteredBy / HasNameserver existem. Consumido pelo Plano 04-05 para fechar o requisito TTP-04 completo.
- Backward compat: testes do crate kri0k-graph todos verdes; nenhuma variante pre-existente removida.
- Clippy strict: zero warnings no kri0k-graph com workspace lints aplicadas (`unwrap_used`, `panic`, `unimplemented` denied).
</success_criteria>

<output>
After completion, create `.planning/phases/04-act-node-ttp-whois/04-01-SUMMARY.md` registrando:
- Variantes adicionadas (NodeKind + EdgeKind)
- Numero de testes adicionados e total no crate
- Confirmacao de clippy verde
- Referencia ao requisito coberto: TTP-04 (parcial — graph types)
</output>
