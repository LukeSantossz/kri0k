---
phase: 01-langgraph-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - python/kri0k/agent/__init__.py
  - python/kri0k/agent/state.py
  - python/kri0k/agent/nodes/__init__.py
  - python/kri0k/agent/nodes/sense.py
  - python/kri0k/agent/nodes/reason.py
  - python/kri0k/agent/nodes/plan.py
  - python/kri0k/agent/nodes/act.py
  - python/kri0k/agent/nodes/reflect.py
  - python/kri0k/agent/graph.py
  - tests/test_agent_graph.py
autonomous: true
requirements:
  - AGENT-01

must_haves:
  truths:
    - "LangGraph StateGraph compiles without error"
    - "Graph contains 5 nodes: sense, reason, plan, act, reflect"
    - "Graph edges form sequence: START->sense->reason->plan->act->reflect->[conditional]"
    - "Placeholder nodes execute without error and return empty dict (except reflect)"
    - "reflect node increments iteration_count"
    - "Router terminates at MAX_ITERATIONS (10)"
    - "All tests pass with pytest"
  artifacts:
    - path: "python/kri0k/agent/state.py"
      provides: "AgentState TypedDict with 7 fields"
      contains: "class AgentState"
    - path: "python/kri0k/agent/graph.py"
      provides: "StateGraph builder and router"
      exports: ["get_graph", "route_after_reflect", "MAX_ITERATIONS"]
    - path: "python/kri0k/agent/nodes/sense.py"
      provides: "sense node placeholder"
      exports: ["sense"]
    - path: "python/kri0k/agent/nodes/reason.py"
      provides: "reason node placeholder"
      exports: ["reason"]
    - path: "python/kri0k/agent/nodes/plan.py"
      provides: "plan node placeholder"
      exports: ["plan"]
    - path: "python/kri0k/agent/nodes/act.py"
      provides: "act node placeholder"
      exports: ["act"]
    - path: "python/kri0k/agent/nodes/reflect.py"
      provides: "reflect node with iteration increment"
      exports: ["reflect"]
    - path: "tests/test_agent_graph.py"
      provides: "Test coverage for graph structure"
      min_lines: 50
  key_links:
    - from: "python/kri0k/agent/graph.py"
      to: "python/kri0k/agent/nodes/__init__.py"
      via: "imports sense, reason, plan, act, reflect"
      pattern: "from kri0k.agent.nodes import"
    - from: "python/kri0k/agent/graph.py"
      to: "python/kri0k/agent/state.py"
      via: "imports AgentState"
      pattern: "from kri0k.agent.state import AgentState"
    - from: "python/kri0k/agent/nodes/*.py"
      to: "python/kri0k/agent/state.py"
      via: "imports AgentState for type annotation"
      pattern: "from kri0k.agent.state import AgentState"
---

<objective>
Create the structural foundation of the LangGraph agent with StateGraph, placeholder nodes (sense/reason/plan/act/reflect), edges connecting them in the correct engagement loop sequence, and conditional routing for iteration control.

Purpose: Establish the graph topology that future phases will populate with real logic. This phase proves the structure works end-to-end before adding complexity.

Output: Compilable StateGraph with placeholder nodes, passing tests, ready for Phase 2 (Sense + Ollama).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-langgraph-structure/01-CONTEXT.md
@.planning/phases/01-langgraph-structure/01-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From python/kri0k/__init__.py (package pattern):
```python
"""Kri0k — AI-driven reconnaissance orchestrator.

This package provides Python bindings to the kri0k Rust core.
"""

from kri0k._native import get_dummy_graph, hello

__version__ = "0.1.0"
__all__ = ["get_dummy_graph", "hello"]
```

From python/kri0k/_native.pyi (type stub pattern):
```python
"""Type stubs for kri0k._native (Rust extension module).

Generated from Rust PyO3 bindings.
"""

from typing import Any

def hello() -> str: ...
def get_dummy_graph() -> dict[str, Any]: ...
```

From tests/test_smoke.py (test pattern):
```python
def test_hello() -> None:
    """Test hello() function from Rust core."""
    result = kri0k.hello()
    assert isinstance(result, str)
    assert "kri0k" in result.lower()
```

From pyproject.toml (type annotation requirements):
- mypy strict mode enabled
- Google docstring convention
- Import order: stdlib, third-party, first-party (kri0k)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AgentState TypedDict and node placeholder files</name>
  <files>
    python/kri0k/agent/__init__.py,
    python/kri0k/agent/state.py,
    python/kri0k/agent/nodes/__init__.py,
    python/kri0k/agent/nodes/sense.py,
    python/kri0k/agent/nodes/reason.py,
    python/kri0k/agent/nodes/plan.py,
    python/kri0k/agent/nodes/act.py,
    python/kri0k/agent/nodes/reflect.py
  </files>
  <read_first>
    - python/kri0k/__init__.py (package init pattern)
    - python/kri0k/_native.pyi (type stub pattern)
    - .planning/phases/01-langgraph-structure/01-PATTERNS.md (all patterns)
    - .planning/phases/01-langgraph-structure/01-CONTEXT.md (decisions D-01 through D-08, D-15, D-16)
    - pyproject.toml (mypy/ruff settings)
  </read_first>
  <action>
    Create directory structure `python/kri0k/agent/` and `python/kri0k/agent/nodes/`.

    Create `python/kri0k/agent/state.py` with `AgentState(TypedDict)` per D-01, D-02:
    - Fields: `snapshot: dict[str, Any]`, `analysis: dict[str, Any]`, `proposal: dict[str, Any]`, `decision: dict[str, Any]`, `iteration_count: int`, `history: list[dict[str, Any]]`, `engagement_context: dict[str, Any]`
    - Module docstring explaining purpose
    - Import from `typing`: TypedDict, Any

    Create 5 node files in `python/kri0k/agent/nodes/` per D-05, D-06, D-07, D-16:
    - Each file: module docstring, import AgentState, single async function
    - Signature: `async def {node}(state: AgentState) -> dict[str, Any]`
    - sense.py, reason.py, plan.py, act.py: return `{}`
    - reflect.py: return `{"iteration_count": state["iteration_count"] + 1}`
    - Google docstrings with Args/Returns sections

    Create `python/kri0k/agent/nodes/__init__.py`:
    - Import all 5 node functions
    - Define `__all__` with exports

    Create `python/kri0k/agent/__init__.py`:
    - Docstring for agent module
    - Import and export `get_graph` from graph.py (created in Task 2)
    - Import and export `AgentState` from state.py
  </action>
  <verify>
    <automated>cd C:\Users\lucas\OneDrive\Desktop\kri0k && python -c "from kri0k.agent.state import AgentState; from kri0k.agent.nodes import sense, reason, plan, act, reflect; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - python/kri0k/agent/state.py contains `class AgentState(TypedDict)`
    - AgentState has 7 fields: snapshot, analysis, proposal, decision, iteration_count, history, engagement_context
    - python/kri0k/agent/nodes/sense.py contains `async def sense(state: AgentState) -> dict[str, Any]`
    - python/kri0k/agent/nodes/reason.py contains `async def reason(state: AgentState) -> dict[str, Any]`
    - python/kri0k/agent/nodes/plan.py contains `async def plan(state: AgentState) -> dict[str, Any]`
    - python/kri0k/agent/nodes/act.py contains `async def act(state: AgentState) -> dict[str, Any]`
    - python/kri0k/agent/nodes/reflect.py contains `async def reflect(state: AgentState) -> dict[str, Any]` and returns `{"iteration_count": state["iteration_count"] + 1}`
    - python/kri0k/agent/nodes/__init__.py exports sense, reason, plan, act, reflect
    - ruff check python/kri0k/agent/ exits 0
    - mypy python/kri0k/agent/ exits 0
  </acceptance_criteria>
  <done>AgentState TypedDict and 5 async node placeholder functions exist, pass ruff and mypy</done>
</task>

<task type="auto">
  <name>Task 2: Create graph.py with StateGraph builder and router</name>
  <files>python/kri0k/agent/graph.py</files>
  <read_first>
    - python/kri0k/agent/state.py (AgentState definition from Task 1)
    - python/kri0k/agent/nodes/__init__.py (node exports from Task 1)
    - .planning/phases/01-langgraph-structure/01-PATTERNS.md (graph.py pattern)
    - .planning/phases/01-langgraph-structure/01-CONTEXT.md (decisions D-09 through D-14, D-17)
  </read_first>
  <action>
    Create `python/kri0k/agent/graph.py` per D-09 through D-14, D-17:

    Imports:
    - `from typing import Any`
    - `from langgraph.graph import END, START, StateGraph`
    - `from kri0k.agent.nodes import act, plan, reason, reflect, sense`
    - `from kri0k.agent.state import AgentState`

    Module-level constant `MAX_ITERATIONS: int = 10` per D-14.

    Router function `route_after_reflect(state: AgentState) -> str` per D-10, D-13:
    - If `state["iteration_count"] >= MAX_ITERATIONS`: return `END`
    - Else: return `"sense"`
    - Google docstring explaining routing logic

    Builder function `get_graph() -> StateGraph` per D-12:
    - Create `StateGraph(AgentState)`
    - Add 5 nodes: sense, reason, plan, act, reflect
    - Add edges: START->sense, sense->reason, reason->plan, plan->act, act->reflect per D-09
    - Add conditional edge from reflect using `route_after_reflect` per D-09
    - Return `graph.compile()`
    - Google docstring with Returns section

    Update `python/kri0k/agent/__init__.py` to import and export `get_graph`.
  </action>
  <verify>
    <automated>cd C:\Users\lucas\OneDrive\Desktop\kri0k && python -c "from kri0k.agent import get_graph; g = get_graph(); print(f'Graph compiled: {type(g).__name__}')"</automated>
  </verify>
  <acceptance_criteria>
    - python/kri0k/agent/graph.py contains `MAX_ITERATIONS: int = 10`
    - python/kri0k/agent/graph.py contains `def route_after_reflect(state: AgentState) -> str`
    - python/kri0k/agent/graph.py contains `def get_graph() -> StateGraph`
    - get_graph() adds nodes: sense, reason, plan, act, reflect (5 total)
    - get_graph() adds edge START -> sense
    - get_graph() adds edges sense->reason, reason->plan, plan->act, act->reflect
    - get_graph() adds conditional edge from reflect using route_after_reflect
    - get_graph() returns compiled graph (calls graph.compile())
    - python/kri0k/agent/__init__.py exports get_graph
    - ruff check python/kri0k/agent/graph.py exits 0
    - mypy python/kri0k/agent/graph.py exits 0
  </acceptance_criteria>
  <done>StateGraph compiles, exports get_graph, passes ruff and mypy</done>
</task>

<task type="auto">
  <name>Task 3: Create test_agent_graph.py with structure and execution tests</name>
  <files>tests/test_agent_graph.py</files>
  <read_first>
    - tests/test_smoke.py (test pattern)
    - python/kri0k/agent/graph.py (get_graph, route_after_reflect, MAX_ITERATIONS)
    - python/kri0k/agent/state.py (AgentState)
    - .planning/phases/01-langgraph-structure/01-PATTERNS.md (test patterns)
    - pyproject.toml (pytest-asyncio marker)
  </read_first>
  <action>
    Create `tests/test_agent_graph.py` with pytest-asyncio for async tests:

    Imports:
    - `import pytest`
    - `from kri0k.agent import get_graph, AgentState`
    - `from kri0k.agent.graph import MAX_ITERATIONS, route_after_reflect`
    - `from kri0k.agent.nodes import sense, reason, plan, act, reflect`

    Test functions:
    1. `test_get_graph_compiles()`: Call get_graph(), assert returns compiled graph (not None)
    2. `test_graph_has_five_nodes()`: Assert graph has nodes sense, reason, plan, act, reflect
    3. `test_agent_state_has_required_fields()`: Assert AgentState has 7 keys (use TypedDict.__annotations__)
    4. `@pytest.mark.asyncio async def test_sense_node_returns_empty_dict()`: Call sense with minimal state, assert returns `{}`
    5. `@pytest.mark.asyncio async def test_reflect_node_increments_iteration()`: Call reflect with iteration_count=5, assert returns `{"iteration_count": 6}`
    6. `test_route_after_reflect_continues_under_max()`: Call router with iteration_count=5, assert returns "sense"
    7. `test_route_after_reflect_ends_at_max()`: Call router with iteration_count=10, assert returns END (import END from langgraph.graph)
    8. `test_max_iterations_is_ten()`: Assert MAX_ITERATIONS == 10

    Helper: Create minimal AgentState fixture dict with all 7 fields set to empty/zero defaults.
  </action>
  <verify>
    <automated>cd C:\Users\lucas\OneDrive\Desktop\kri0k && pytest tests/test_agent_graph.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_agent_graph.py contains at least 8 test functions
    - test_get_graph_compiles passes
    - test_graph_has_five_nodes passes (verifies sense, reason, plan, act, reflect)
    - test_agent_state_has_required_fields passes (7 fields)
    - test_sense_node_returns_empty_dict passes (async test)
    - test_reflect_node_increments_iteration passes (async test)
    - test_route_after_reflect_continues_under_max passes (returns "sense")
    - test_route_after_reflect_ends_at_max passes (returns END)
    - test_max_iterations_is_ten passes
    - pytest tests/test_agent_graph.py exits 0
    - ruff check tests/test_agent_graph.py exits 0
  </acceptance_criteria>
  <done>All 8+ tests pass, test file passes ruff</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This phase creates placeholder code with no external inputs or execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information Disclosure) | AgentState TypedDict | accept | State is internal Python structure; no sensitive data in Phase 1 placeholders |
| T-01-02 | D (Denial of Service) | route_after_reflect | mitigate | MAX_ITERATIONS=10 hardcoded prevents infinite loop; enforced by router |

Note: Phase 1 creates structural placeholders only. Real security boundaries (scope validation, audit logging) are implemented in Phases 7-8 per ROADMAP.
</threat_model>

<verification>
1. Directory structure exists: `python/kri0k/agent/`, `python/kri0k/agent/nodes/`
2. All 10 Python files created and pass ruff/mypy
3. `from kri0k.agent import get_graph, AgentState` succeeds
4. `get_graph()` returns compiled StateGraph
5. `pytest tests/test_agent_graph.py` passes all tests
6. Full test suite: `pytest tests/` passes (including existing smoke tests)
</verification>

<success_criteria>
- [ ] `python/kri0k/agent/graph.py` exists with StateGraph defined
- [ ] Nodes sense, reason, plan, act, reflect registered in graph
- [ ] Edges connect nodes in sequence: START->sense->reason->plan->act->reflect->[conditional]
- [ ] `pytest tests/test_agent_graph.py` passes (all 8+ tests)
- [ ] `ruff check python/kri0k/agent/` exits 0
- [ ] `mypy python/kri0k/agent/` exits 0
- [ ] Requirement AGENT-01 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-langgraph-structure/01-01-SUMMARY.md`
</output>
