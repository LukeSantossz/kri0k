# KRK-001 Kri0K — Clippy configuration
# Enforced via CI with -D warnings (deny all warnings)
#
# NOTE: Lint levels (deny/warn/allow) are configured in the root Cargo.toml
#       under [workspace.lints.clippy], NOT in this file.
#
# This file is for clippy-specific threshold/configuration options only.
# Valid options: https://doc.rust-lang.org/clippy/lint_configuration.html
#
# Run with: cargo clippy --all-targets --all-features -- -D warnings

# Currently using default thresholds.
# Uncomment and adjust if needed:
# cognitive-complexity-threshold = 30
# too-many-arguments-threshold = 7
