# KRK-001 Kri0K — Rustfmt configuration
# Enforced via CI and pre-commit hooks

# Edition and MSRV
edition = "2021"

# Formatting options (stable)
max_width = 100
hard_tabs = false
tab_spaces = 4
reorder_imports = true
use_small_heuristics = "Default"
force_explicit_abi = true
match_block_trailing_comma = true
use_field_init_shorthand = true
chain_width = 60

# Nightly-only options (uncomment when using `cargo +nightly fmt`):
# NOTE: These options require nightly Rust and will emit warnings on stable.
# To use: `rustup install nightly && cargo +nightly fmt`
#
# imports_granularity = "Crate"
# group_imports = "StdExternalCrate"
# fn_single_line = false
# where_single_line = false
# wrap_comments = true
# format_code_in_doc_comments = true
# normalize_comments = true
# normalize_doc_attributes = true
# format_strings = true
# format_macro_matchers = true
# format_macro_bodies = true
# trailing_semicolon = true
# trailing_comma = "Vertical"
